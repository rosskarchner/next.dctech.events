{"_type":"issue","id":"next_dctech_events-udg","title":"db.py drops skip_phrases when reading GROUP items","description":"_group_item_to_dict in cdk_next/lambda_src/api/db.py copies a fixed allowlist of fields off the GROUP# item:\n\n    for field in ['website', 'ical', 'fallback_url', 'categories',\n                  'suppress_urls', 'suppress_guid', 'scan_for_metadata', 'url_override']:\n\nskip_phrases is absent, so the field is silently discarded on every read even though put_group writes it and the migration carried it over from _groups/*.yaml (nvtc.yaml sets it). Anything reading groups through this helper — the site-generator export, the iCal aggregator, and the MCP list_groups tool — never sees it.\n\n## Context\nIntroduced when backend/db.py was forked for this stack. One-line fix, but it blocks any skip_phrases support in calgen from taking effect.","acceptance_criteria":"skip_phrases survives a get_group/get_all_groups round-trip and reaches _groups/*.yaml via export_dynamo_to_calgen.py.","status":"open","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:39:00Z","created_by":"Ross Karchner","updated_at":"2026-08-02T12:39:00Z","labels":["api","bug","dynamodb"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-7lg","title":"Cap iCal fetch horizon at 180 days","description":"The vendored calgen has no upper bound on how far ahead it keeps iCal events, so dctech.events currently lists events out to 2029. The pre-cutover site capped them at 180 days.\n\nIn packages/calgen/src/calgen/calendars.py, fetch_ical_and_extract_events expands each VEVENT with an rrule individually over a 90-day window (recurring_end_dt) but applies no horizon at all to non-recurring events. Upstream replaced this with a single whole-calendar expansion bounded at 180 days:\n\n    start_dt = datetime.now(timezone.utc)\n    end_dt = start_dt + timedelta(days=180)\n    for event in recurring_ical_events.of(cal).between(start_dt, end_dt):\n\nMeasured against the live AFCEA feed: the current code yields 76 events through 2027-06, the 180-day form yields 39 through 2027-01.\n\n## Context\nBehaviour exists in the now-archived rosskarchner/calgen at origin/main (commit ce08c53 lineage), fetchable from the local checkout at ~/projects/calgen. Never ported when calgen was vendored, because the vendored copy came from a local working tree that predated it.","acceptance_criteria":"Events beyond 180 days no longer appear on dctech.events. The iCal aggregator's prune step removes the now-out-of-horizon EVENT# items on its next run (expect roughly 96 removals). Site build succeeds and the homepage/month pages still render.","status":"open","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:38:25Z","created_by":"Ross Karchner","updated_at":"2026-08-02T12:38:25Z","labels":["calgen","ical","site-content"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-xyy","title":"Normalize invisible Unicode in event titles","description":"Event titles arriving from iCal feeds can contain zero-width and other invisible Unicode characters. Left in place they break two things:\n\n- skip_phrases matching, since a title with a zero-width space no longer matches the configured phrase\n- duplicate detection, since are_events_duplicates in packages/calgen/src/calgen/pipeline.py compares titles exactly, and calculate_event_hash in event_utils.py folds the title into the guid — so the same event can churn guids or fail to dedupe\n\nUpstream strips these characters in event_utils.py before hashing and in pipeline.py before comparison.\n\n## Context\nBehaviour exists in the now-archived rosskarchner/calgen at origin/main (commit 3c9246e, 'Fix invisible Unicode chars breaking skip_phrases filtering and duplicate detection'), fetchable from ~/projects/calgen. Note this changes guids for any affected event, so expect one-time churn: the aggregator will write new EVENT# items and prune the old ones. Upstream also ships tests/test_unicode_normalization.py worth porting alongside.","acceptance_criteria":"Titles differing only by invisible characters produce the same guid and dedupe against each other; skip_phrases matching is unaffected by them.","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:39:36Z","created_by":"Ross Karchner","updated_at":"2026-08-02T12:39:36Z","labels":["calgen","data-quality"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-jbv","title":"Port skip_phrases group filtering into calgen","description":"Groups can declare skip_phrases to drop matching events from their feed. The vendored calgen ignores the field entirely, so those events publish. _groups/nvtc.yaml sets:\n\n    skip_phrases:\n      - COMMUNITY TUESDAYS AT REFRACTION\n\nand roughly 15 such events are currently live on dctech.events; the pre-cutover site filtered them.\n\nUpstream implements the match in packages/calgen/src/calgen/pipeline.py during event processing, case-insensitively against the event title.\n\n## Context\nBehaviour exists in the now-archived rosskarchner/calgen at origin/main (commit ce08c53), fetchable from ~/projects/calgen. Blocked by the db.py read bug — until that is fixed the field never reaches calgen even if the matching logic is ported.","acceptance_criteria":"Events whose titles match a group's skip_phrases are absent from _data/all_events.json and the built site. The nvtc 'Community Tuesdays at Refraction' events stop appearing.","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:39:21Z","created_by":"Ross Karchner","updated_at":"2026-08-02T12:39:21Z","labels":["calgen","site-content"],"dependencies":[{"issue_id":"next_dctech_events-jbv","depends_on_id":"next_dctech_events-udg","type":"blocks","created_at":"2026-08-02T08:39:21Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
