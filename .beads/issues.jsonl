{"_type":"issue","id":"next_dctech_events-6r6","title":"CORS wildcard fallback leaks to /api/admin/* responses","description":"cdk_next/lambda_src/api/handler.py's get_cors_origin() returns '*' for any Origin outside the 3-item allowlist, and add_cors() applies this to every response including /api/admin/*. A comment claims it's 'restricted below' but no route actually narrows it back. Any site can read admin JSON responses from a browser holding a leaked/stolen bearer token. Fix: default-deny (no CORS header) instead of wildcard for unrecognized origins.","status":"open","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:02Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:02Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-326","title":"SSRF risk: iCal aggregator fetches unvalidated URLs","description":"packages/calgen/src/calgen/calendars.py (fetching group iCal URLs and scraped event page URLs) relies on db.py's is_safe_url(), which only rejects non-http(s) schemes -- http://169.254.169.254/latest/meta-data/... or http://localhost/... pass through unchanged. A malicious or compromised group feed could trigger SSRF against the Lambda's metadata endpoint or internal network on every aggregator cycle. Fix: block private/link-local IP ranges, including after DNS resolution to stop DNS-rebinding, before fetching.","status":"open","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:01Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:01Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-ph2","title":"Fix: CORRECTION# items leaked into the DRAFT# queue (GSI1/GSI3 collision)","description":"get_drafts_by_status/get_drafts_by_submitter queried GSI1PK=STATUS#{status} / GSI3PK=USER#{id} with no PK-prefix filter, and CORRECTION# items (added by the corrections feature) use the identical convention -- a pending correction showed up in the admin Pending Submissions queue as an Untitled/no-details row, and clicking Reject 404'd ('Draft not found') because get_draft looks up PK=DRAFT#{id}, which doesn't exist for a correction stored at PK=CORRECTION#{id}. Found live in prod via user report + screenshot (queue.html showing a stuck 'Untitled' submission that couldn't be rejected). The corrections-side queries (get_corrections_by_status/by_submitter) already had the mirror-image filter; the drafts-side queries just never got the reverse fix. Fixed by adding the same item['PK'].startswith('DRAFT#') filter to both draft-query functions. 2 new regression tests in test_corrections.py. Full suite: 340 passing. Cleaned up the stray CORRECTION# item live via reject_correction.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T12:45:14Z","created_by":"Ross Karchner","updated_at":"2026-09-01T12:45:20Z","closed_at":"2026-09-01T12:45:20Z","close_reason":"Fixed and deploying.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-uwk","title":"Public corrections to published events","description":"# Public corrections to published events\n\n## Context\n\nEvents on dctech.events come from three sources: an iCal feed (`source: 'ical'`), a manual entry, or a promoted public submission (`source: 'submitted'`). Once live, the only way to fix a wrong detail today is an admin hand-editing it in the `/edit/events.html` overlay UI — there's no way for a member of the public (an organizer, an attendee) to flag or fix something themselves. We want a lightweight \"suggest a correction\" path that lands in a moderation queue rather than writing directly to the live event, so nothing public-submitted goes live unreviewed.\n\nThe one hard constraint: for iCal-sourced events, only `description`, venue (`location`), and `url` may be corrected — the date/time must stay whatever the iCal feed says, since that feed is the authoritative source and gets re-fetched independently. Investigating the existing overlay system (`db.py`'s `set_event_overlay`) turned up a useful fact: **`date`/`end_date` are already permanently un-overlayable for every event regardless of source** (`OVERLAY_PROTECTED_FIELDS`) — so date-locking needs no new code at all. The only field that actually needs new, source-conditional locking is `time`/`end_time`, which today *is* overlay-editable for every source, including iCal.\n\nOutcome: a public correction form linked from each event page, a moderation queue for admins to approve/reject, and — once approved — the correction is applied through the exact same `set_event_overlay` writer an admin hand-edit already uses, so there's no second code path for \"how an event's displayed fields get changed.\"\n\n## Approach\n\nA correction is a **new, separate pending entity** (`CORRECTION#{id}`), never written into an event's `overrides` until a moderator approves it. Approval is a thin wrapper around the existing `db.set_event_overlay(...)` — so once approved, a correction is indistinguishable from an admin hand-edit. Identity is handled by reusing `routes/submit.py`'s existing `_resolve_submitter` (magic link or Cognito) — no new auth code. The whole thing closely mirrors the existing DRAFT#/submission pipeline, reusing its shape wherever it fits rather than inventing a new pattern.\n\n### Data model\n\n`CORRECTION#{uuid4[:8]}` item, modeled on `DRAFT#`'s shape (`db.py:255` `create_draft`):\n```\nPK: CORRECTION#{id}          SK: META\nGSI1PK: STATUS#{status}      GSI1SK: {created_at}   # 'pending'|'approved'|'rejected'\nGSI3PK: USER#{submitter_id}  GSI3SK: {created_at}\n\ntarget_guid, target_source (snapshot at submission), target_title (denormalized, cosmetic)\nfields: {field: new_value}   # only changed fields, already allowlist- and type-checked\nreason: str                  # required — submitter's note on what's wrong\nsubmitter_email, submitter_id, created_at, status\nreviewer_email, reviewed_at, rejection_reason   # set on resolution\n```\n\n**Important:** `DRAFT#` items already use the identical `GSI1PK = STATUS#{status}` / `GSI3PK = USER#{id}` convention. A query for `STATUS#pending` on GSI1 will return both drafts and corrections interleaved unless filtered. `db.py` already has this exact problem solved for `CANDIDATE#` items sharing GSI5 with `EVENT#` items — `get_candidates()` (db.py:1377-1384) post-filters on `item['PK'].startswith('CANDIDATE#')`. Do the same in `get_corrections_by_status`/`get_corrections_by_submitter`.\n\n### Field allowlist (the core business rule)\n\nNew constants in `db.py`, next to `OVERLAY_EDITABLE_FIELDS`/`OVERLAY_PROTECTED_FIELDS` (db.py:726-739):\n```python\nICAL_CORRECTION_FIELDS = ('description', 'location', 'url')\nOTHER_CORRECTION_FIELDS = ('description', 'location', 'url', 'time', 'end_time')\n\nCORRECTION_EDITABLE_FIELDS_BY_SOURCE = {\n    'ical': ICAL_CORRECTION_FIELDS,\n    'manual': OTHER_CORRECTION_FIELDS,\n    'submitted': OTHER_CORRECTION_FIELDS,\n}\n\ndef correction_allowed_fields(source):\n    # Unknown/future source values fall back to the narrower iCal set — fail\n    # closed, not open.\n    return CORRECTION_EDITABLE_FIELDS_BY_SOURCE.get(source, ICAL_CORRECTION_FIELDS)\n\ndef check_correction_fields(source, fields):\n    # Raises ValueError naming any field not in correction_allowed_fields(source).\n    # Then reuses the existing _check_overlay_types(fields) so a correction can't\n    # smuggle in a malformed value any more than an admin edit can.\n```\n`date`/`end_date` need no special-casing — already unconditionally rejected by `set_event_overlay`'s existing `OVERLAY_PROTECTED_FIELDS` check for every source.\n\n### `db.py` additions (next to the DRAFT#/overlay functions, same reasoning as `build_event_draft_data` living there: `build_lambdas.sh` bundles `db.py` into the MCP Lambda but not the route modules)\n\n- `correction_allowed_fields(source)`, `check_correction_fields(source, fields)` — above.\n- `create_correction(target_guid, fields, reason, submitter_email, submitter_id=None)` — validates target exists via `get_event_from_config`, snapshots `source`/`title`, writes the item, returns the id.\n- `get_correction(correction_id)`, `get_corrections_by_status(status='pending')`, `get_corrections_by_submitter(user_id)` — mirror `get_draft`/`get_drafts_by_status`/`get_drafts_by_submitter`, with the PK-prefix filter noted above.\n- `approve_correction(correction_id, reviewer_email)` — fetches the correction, **re-fetches the target event live** (not the `target_source` snapshot) and re-runs `check_correction_fields` against its *current* source (an admin could have re-sourced the event since submission); raises `ValueError` if the event is gone or the fields are no longer all allowed, leaving the correction `pending` in either case (never auto-rejects on a transient failure). On success: `set_event_overlay(target_guid, fields, comment=f\"Public correction from {submitter_email}: {reason}\", actor=reviewer_email)` (default forgiving `expected_rev` — no live human to show a conflict to, same reasoning `set_event_overlay`'s own docstring gives for bulk/agent writes), then marks the correction `approved`.\n- `reject_correction(correction_id, reviewer_email, reason=None)` — marks `rejected`, no overlay write.\n\n### Route layer: `cdk_next/lambda_src/api/routes/corrections.py` (new)\n\nPublic:\n- `submit_correction_json` → `POST /api/corrections`. Reuses `_resolve_submitter` from `routes/submit.py` verbatim. Validates: guid present and resolves via `get_event_from_config` (404 if not); `fields` a non-empty dict; `reason` non-blank (required, mirroring `routes/events.py`'s non-empty-`comment` rule on the admin overlay endpoint); `check_correction_fields(source, fields)` (422 on `ValueError`). Then `create_correction(...)`, return 201 with the id.\n- `get_public_event_json` → `GET /api/public/events/{guid}` (**new endpoint** — there is no existing unauthenticated single-event read). Returns `db.effective_event(record)`'s display fields (title, date, time, end_time, location, url, description, source, hidden, duplicate_of) plus `correctable_fields: correction_allowed_fields(source)`. 404 if no `EVENT#{guid}` row — this is also what a recurring-event occurrence guid hits, since those have no stable row (see Out of scope).\n\nAdmin (all call `auth.require_admin` exactly like every other admin route):\n- `list_corrections_json` → `GET /api/admin/corrections?status=pending`.\n- `get_correction_json` → `GET /api/admin/corrections/{id}`, enriched with the *live* `target_event` so a moderator sees current hidden/duplicate_of/source state, not just the submission-time snapshot.\n- `approve_correction_json` → `POST /api/admin/corrections/{id}/approve`. 404 on not-found/deleted-target, 422 on the current-source re-validation failure.\n- `reject_correction_json` → `POST /api/admin/corrections/{id}/reject`, optional `reason` in the body.\n\nWire all of these into `handler.py`'s route table (plain `if path ==` dispatch, same style as the existing blocks around line 106-249).\n\n### CDK: `cdk_next/stacks/api_stack.py`\n\nVerified the `/api` resource tree (api_stack.py:199-216) has **no** blanket proxy — only explicit children (`events`, `categories`, `submit-link`, `submissions`, `my-submissions`) plus `admin` which *is* a full proxy (`add_proxy(...)`). So `/api/admin/corrections*` needs **no** CDK change, but the two new public routes do:\n```python\napi_res.add_resource(\"corrections\").add_method(\"POST\", integration)\n\npublic_res = api_res.add_resource(\"public\")\npublic_res.add_resource(\"events\").add_resource(\"{guid}\").add_method(\"GET\", integration)\n```\nSame \"unauthenticated at the gateway, Lambda verifies the magic link itself\" reasoning already documented at api_stack.py:203-206 for `submissions`.\n\n### Magic-link redirect support\n\nToday `magic_link.build_link(email, timestamp, signature, path='/edit/submit-event.html')` (magic_link.py:106) is always called with the default path (`routes/submit.py` `request_link_json`, ~line 296, never passes `path=`). Two small changes:\n\n1. `magic_link.build_link` — make it query-string-aware (append `\u0026` instead of `?` if `path` already has a `?`), so a path can carry `?guid=...`.\n2. `request_link_json` — accept an optional `redirect_path` in the POST body, validated against an explicit allowlist (`/edit/submit-event.html`, `/edit/correct-event.html`), with a strict `guid` regex (`^[0-9a-f]{1,64}$`) if present. Anything else — a URL with a scheme/host, an unrecognized path — silently falls back to the default submission path rather than erroring (a bad redirect must never cost the user their magic link). Pass the sanitized result to `build_link(..., path=redirect_path)`.\n\nNo change to `check_and_record_link_request`'s rate limiting or the KMS token itself — both are already generic over destination.\n\n### Frontend\n\n- `frontends/edit/correct-event.html` + `frontends/edit/js/correction.js` (new) — mirrors `submit-event.html`/`js/submission.js`'s magic-link-gated shell exactly (copy `readMagicToken`/`stripTokenFromUrl`/`requestLink`). On load with a resolved identity: `GET /api/public/events/{guid}` (guid from `?guid=`); 404 → friendly \"can't be corrected here\" message (covers the recurring-event case). Otherwise render current-value/proposed-value pairs for `description`/`location`/`url` always, and `time`/`end_time` only when present in `correctable_fields` (otherwise show them as read-only text with a one-line explanation that they come from the organizer's calendar). Required `reason` textarea. Client-side diff before POST to `/api/corrections` with `{guid, fields, reason}` — send only fields that actually changed, block submit if nothing changed.\n- `frontends/edit/corrections.html` + `frontends/edit/js/corrections.js` (new) — mirrors `queue.html`/`js/queue.js`'s list + approve/reject pattern (`DctechAuth.requireAdmin()` guard, `loadCorrections()` → `GET /api/admin/corrections?status=pending`, row shows target event + submitter + reason + diff of proposed vs. current values, Approve/Reject buttons). Should-have: visually group multiple pending corrections against the same `target_guid` so a moderator doesn't approve two conflicting ones without noticing (not a backend constraint — `set_event_overlay` just applies each in sequence, last-approved-wins per field, same as two admin edits minutes apart).\n- Add a `Corrections` nav entry to the `data-auth=\"admin\"` nav list duplicated across `queue.html`, `events.html`, `posts.html`, `subscribers.html`, and the new `corrections.html`.\n\n### calgen template: `site/templates/event_page.html`\n\nConfirmed the `event-actions` paragraph is the last element in the article (lines 107-111) and every rendered event (including recurring occurrences, `pipeline.py:263`) already carries a `guid` key. Add, guarded by the same `is_recurring` flag the page already checks at line 47:\n```jinja\n{% if not event.is_recurring %}\n\u0026middot;\n\u003ca href=\"/edit/correct-event.html?guid={{ event.guid }}\"\u003eReport an issue with this event\u003c/a\u003e\n{% endif %}\n```\nRecurring occurrences get suppressed entirely rather than shown-then-404, since their computed guid has no matching `EVENT#{guid}` row.\n\n### MCP tool parity\n\nAdd `list_pending_corrections`, `get_correction`, `approve_correction`, `reject_correction` to `cdk_next/lambda_src/mcp/server.py`, mirroring `approve_submission`/`reject_submission` (server.py:597-650) — trivial wrappers since `db.py` is already bundled into the MCP Lambda. Worth doing so \"what needs review\" stays a single concept reachable from one surface, rather than corrections becoming a second, MCP-invisible queue.\n\n### Edge cases (handled per above, listed for visibility)\n\n1. Event hidden/merged between submission and review — submission doesn't block on this; approval doesn't either (writing an overlay onto a hidden event is harmless), but the admin detail view shows live state so a moderator sees it.\n2. Event deleted before review — `approve_correction` 404s, correction stays `pending` (not auto-rejected) so the reason is visible.\n3. Two pending corrections on the same event — no backend conflict (sequential overlay merge), addressed as a queue-UI affordance only.\n4. Event's `source` changes between submission and review — `approve_correction` re-validates against the *current* source, not the submission-time snapshot; 422 + stays pending if no longer allowed.\n\n### Out of scope\n\n- **Recurring-event corrections** — no stable per-occurrence `EVENT#{guid}` row exists to key an overlay on (`pipeline.py:process_events` computes occurrence guids at build time only). Link suppressed on those pages; public read endpoint 404s helpfully if reached anyway.\n- **\"My corrections\" self-service status page** (parity with `my-submissions.html`) — `get_corrections_by_submitter` gets written since it's nearly free given the GSI3 wiring, but no route/page consumes it yet.\n- **Trusted-submitter auto-approval** — corrections always queue regardless of submitter trust, per \"should go to the moderation queue.\"\n\n## Verification\n\n**Unit tests** — new `cdk_next/lambda_src/api/test_corrections.py`, reusing `test_overlay.py`'s fixture style (`store` fixture stubbing `db.get_event_from_config`/`db.update_event`):\n- `correction_allowed_fields`/`check_correction_fields` per-source allowlisting, including the type-check passthrough and the unknown-source fail-closed case.\n- `create_correction` + `get_corrections_by_status('pending')` do **not** pick up a co-mingled `DRAFT#` item with the same status (the GSI1-collision regression test).\n- `approve_correction` happy path writes through `set_event_overlay` and marks `approved`; failure paths (not found, already resolved, target deleted, source changed and fields no longer allowed) all raise and leave `status` unchanged at `pending`.\n- `reject_correction` happy path and not-found/already-resolved.\n\nRun via `DYNAMODB_TABLE_NAME=t python -m pytest cdk_next/lambda_src/api/test_corrections.py`.\n\n**End-to-end**, once deployed (mirrors this session's `mcp_sigv4_bridge.py`/curl approach):\n1. `POST /api/submit-link` with a `redirect_path` carrying a guid; confirm the emailed link round-trips it, and that a bogus `redirect_path` falls back to the submission page.\n2. `POST /api/corrections` against a manual-sourced event with a `time` change → succeeds; the same against a known iCal-sourced event → 422.\n3. Inspect the written `CORRECTION#{id}` item directly in DynamoDB for the shape above.\n4. As admin, `GET /api/admin/corrections?status=pending` shows it; confirm `/api/admin/queue` (drafts) is unaffected (proves the GSI collision fix works against real data).\n5. `POST /api/admin/corrections/{id}/approve`; confirm via `GET /api/admin/events/{guid}` that `overrides` now has the field, `_edited_by` is the admin, `_comment` names the submitter/reason.\n6. Trigger a rebuild, confirm the corrected field renders on the live event page.\n7. Confirm `GET /api/public/events/{guid}` 404s for a recurring occurrence's guid, and that its event page has no \"Report an issue\" link.\n\n### Critical files\n- `cdk_next/lambda_src/api/db.py`\n- `cdk_next/lambda_src/api/routes/submit.py`\n- `cdk_next/lambda_src/api/routes/corrections.py` (new)\n- `cdk_next/lambda_src/api/handler.py`\n- `cdk_next/stacks/api_stack.py`\n- `cdk_next/lambda_src/mcp/server.py`\n- `frontends/edit/js/submission.js`, `frontends/edit/js/queue.js` (patterns to mirror)\n- `site/templates/event_page.html`","notes":"Deployed to prod (NextApiStack, NextSiteGeneratorStack, /edit/ UI). E2E verified via Playwright against live site using temp Cognito credentials (aws cognito-idp) + self-generated magic-link tokens (aws kms generate-mac, correct key a14b4b79-1183-43f6-a163-4a12af17d0a2 -- memory's recorded key id was stale): public submission form (magic-link path) end-to-end, admin corrections queue listing with same-target conflict warning, approve (applies overlay correctly incl. attribution/comment), reject (records reason, no overlay write), corrected field renders live after rebuild, Report-an-issue link renders on live event page. Throwaway test event + temp Cognito user cleaned up. Found and filed separately (next_dctech_events-1k7, not fixed here): signed-in-without-magic-link users get 401 on /api/submissions and /api/corrections because those routes are gateway-unauthenticated and get_user_from_event only trusts API Gateway's Cognito authorizer claims -- pre-existing gap shared with the submission flow, needs real JWT/JWKS verification to fix properly.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-09-01T11:18:01Z","created_by":"Ross Karchner","updated_at":"2026-09-01T12:19:35Z","started_at":"2026-09-01T11:18:05Z","closed_at":"2026-09-01T12:19:35Z","close_reason":"Implemented, deployed to prod, and E2E-verified via Playwright (public submission, admin approve/reject, live render). Test fixtures cleaned up.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-4rd","title":"Verify the first live Monday state machine run (2026-08-31)","description":"First scheduled execution of dctech-events-next-monday is Monday 2026-08-31 at\n09:15 UTC. Everything below has been tested in pieces but never as one live\nchain, so watch it once.\n\nWHAT TO CHECK, in order:\n\n1. The QC task-token release. This is the untested link. The agent calls\n   send_task_success itself (calendar_qc/main.py report_to_step_functions);\n   nothing has ever exercised it against a real execution. If it fails, the\n   RunQualityControl step sits until its 1h timeout and then falls through the\n   States.ALL catch — the chain still finishes, but QC is silently skipped.\n   Symptom: execution succeeds, QC digest arrives, but the step shows a\n   TaskTimedOut rather than a Succeeded.\n\n2. That the trigger Lambda does NOT log \"Status: timeout\". It should return in\n   ~10s with status \"started\". If it times out again, the read-timeout fix\n   regressed and item 1 will fail the way described above.\n\n3. Only ONE QC pass runs. retry_attempts is 0 now; three concurrent passes on\n   2026-08-26 starved each other of Bedrock throughput until they died.\n\n4. Both codebuild:startBuild.sync steps complete rather than queueing past\n   their 30m task timeout. concurrent_build_limit is now 1, so a build racing\n   the stream-fed trigger's build will QUEUE — which is correct, but it eats\n   into that 30 minutes.\n\n5. The week-ahead post's event_count matches what /week/2026-W36/ actually\n   shows. Getting this right is the whole reason the chain exists: the post is\n   published after QC and after a rebuild, so its frozen count should no longer\n   include events QC hid that morning.\n\n6. The newsletter goes out once, after the post is live. Its own rule is\n   DISABLED, not deleted — if the machine fails before SendNewsletter, nobody\n   gets a newsletter that week and re-enabling that rule is the fallback.\n\nTIMING: the chain replaces rules that ran at 09:00 / 10:30 / 11:00 UTC. Expect\nthe newsletter around 10:00-10:30 rather than 11:00, since it no longer has to\nleave slack for a QC pass whose finish nothing could observe. The 2026-08-26\ndry run took 713s for a full 36-event queue, so budget ~15 min for QC.\n\nWatch it at https://console.aws.amazon.com/states/ — execution name becomes the\nQC run id, which is what revert_qa_run takes if the pass gets something wrong.","status":"open","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-26T15:09:35Z","created_by":"Ross Karchner","updated_at":"2026-08-26T15:09:35Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-wm4","title":"Monday 'week ahead' link post on /updates/","description":"DONE (not deployed). New 'link' post kind: Monday 10:30 UTC rule invokes updates-publisher with {mode: week_ahead}, writing UPDATE#{date} with post_kind=link + link_url=/week/\u003ciso-week\u003e/, no events, no week_start. Link posts own NO page under /updates/ — calgen sets url=link_url and get_paged_update_posts() gates the route/freezer/sitemap, so /updates/\u003cy\u003e/\u003cm\u003e/\u003cd\u003e/ 404s for them. Social publisher syndicates the target (_syndicated_url), falling back to /week/\u003cweek_id\u003e/ never the dead permalink.\n\nALSO reworked _archive_item: captures now MERGE instead of overwrite, split at today (stored wins before today, live wins today-onward — keeps past events, respects cancellations, no dedupe needed since halves are disjoint by date). Every run refreshes this week AND next week (_archive_weeks). Removes the old destructive-overwrite guard entirely. Verified on prod data: W35 stored=23 live=18 -\u003e merged=27, recovering 4 events the old one-shot capture had permanently missed.\n\nTests: 59 publisher, 40 social, 115 calgen. cdk synth clean; WED rule keeps logical ID NextUpdatesPublishSchedule7866EBFC.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-26T12:26:13Z","created_by":"Ross Karchner","updated_at":"2026-08-26T15:09:34Z","started_at":"2026-08-26T12:26:55Z","closed_at":"2026-08-26T15:09:34Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-rv2","title":"Implement discovery agent Option B","description":"Replace the discovery stub with AgentCore runtime architecture from docs/discovery-agent-tutorial.md Option B: MCP proposal tools, discovery agent package, trigger lambda, stack rewrite, build wiring, app wiring, tests, and deployment.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-24T11:29:04Z","created_by":"Ross Karchner","updated_at":"2026-08-24T12:25:32Z","started_at":"2026-08-24T11:29:07Z","closed_at":"2026-08-24T12:25:32Z","close_reason":"Implemented and deployed discovery Option B architecture: MCP proposal tools, AgentCore discovery runtime package, trigger Lambda, stack rewrite, build/app wiring, tests, and live dry-run validation.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.6","title":"SEO: sitemap.xml omits page families and fakes lastmod","description":"app.py:354-395 omits /{year}/{month}/ (generated at freeze.py:20), /categories/\u003cslug\u003e/\u003cyear\u003e/\u003cmonth\u003e/ (freeze.py:54) and /feeds/. Separately every non-updates URL gets lastmod = build time, which trains crawlers to discount the field.","acceptance_criteria":"Sitemap covers every indexable frozen route. lastmod reflects real content change or is omitted where it cannot be known honestly.","status":"closed","priority":1,"issue_type":"bug","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:28Z","created_by":"Ross Karchner","updated_at":"2026-08-13T22:01:40Z","started_at":"2026-08-13T21:59:28Z","closed_at":"2026-08-13T22:01:40Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.6","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:28Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.5","title":"SEO: /just-added/ is linked from the homepage but has no route","description":"homepage.html:23 links to /just-added/ and site/templates/just_added.html exists, but there is no @app.route for it and no freezer generator. On a frozen static site that is a hard 404 from the most-crawled page.","acceptance_criteria":"Either the route + freezer generator + sitemap entry exist and /just-added/ builds, or the homepage link is removed. No dead internal links.","notes":"CORRECTION to the audit finding: /just-added/ is NOT a live 404. The homepage link at homepage.html:23 sits inside {% if recently_added %}, and 'recently_added' is never populated anywhere in calgen (grep: only the two homepage.html references exist). Verified against a frozen build - the string 'just-added' appears nowhere in the output HTML. So there is no crawlable broken link and no SEO harm today.\n\nWhat is actually here is a half-built feature: just_added.html is complete, the homepage preview block is complete, .content-container/.added-day CSS is in main.css, but there is no route, no freezer generator, and no data source.\n\nBLOCKER on finishing it: the page needs a per-event 'when was this added' date. Every event does have one - DynamoDB EVENT#\u003cguid\u003e/SK=META carries createdAt for ical-sourced events too - but export_dynamo_to_calgen.py:109 lists createdAt in the 'internal' drop tuple, so it never reaches calgen. Worse, ical events are not written to _single_events at all (only source in manual/submitted/None), so un-dropping the field is not sufficient; the export would need a new artifact such as _data/added_at.json mapping guid -\u003e createdAt across all EVENT# META items.\n\nThat is exactly the index cdk_next/lambda_src/updates_publisher/app.py:_added_at_index already builds for the weekly /updates post (bead next_dctech_events-hiy, currently uncommitted work in progress). It also needs that file's (date,title) fallback, because submitted events get a different guid on the site than in DynamoDB (bead next_dctech_events-p8o).\n\nDECISION: left the template, CSS and homepage block untouched rather than deleting a feature that is mid-flight elsewhere, and did not build a second createdAt pipeline alongside hiy. No SEO work is outstanding here. Landmine to note: whoever populates 'recently_added' turns homepage.html:23 into a real 404 unless the route lands in the same change.","status":"closed","priority":1,"issue_type":"bug","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:27Z","created_by":"Ross Karchner","updated_at":"2026-08-14T02:26:05Z","started_at":"2026-08-13T21:57:53Z","closed_at":"2026-08-14T02:26:05Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.5","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:27Z","created_by":"Ross Karchner","metadata":"{}"},{"issue_id":"next_dctech_events-u4j.5","depends_on_id":"next_dctech_events-hiy","type":"related","created_at":"2026-08-13T17:59:21Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.4","title":"SEO: fix H1 structure sitewide","description":"base.html:66 makes the site logo an h1 on every page. Category, location, virtual, week and updates pages therefore have no page-specific h1 (their real heading is an h2 - location_page.html:8, category_page.html:10, virtual_events.html:10), while month_page.html:8 adds its own h1 and ships two.","acceptance_criteria":"Exactly one h1 per page, carrying that page's topic. Logo demoted to a non-heading element (or h1 only on the homepage). Heading order otherwise unbroken.","status":"closed","priority":1,"issue_type":"bug","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:12Z","created_by":"Ross Karchner","updated_at":"2026-08-13T21:57:52Z","started_at":"2026-08-13T21:56:34Z","closed_at":"2026-08-13T21:57:52Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.4","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:12Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.3","title":"SEO: give every page family a unique meta description","description":"Only week_page, update_post, free_post and updates_index override {% block description %}. Category, location, month, category-month, virtual, groups, categories-index and locations-index all inherit the same tagline from base.html:21, so dozens of pages ship an identical meta description.","acceptance_criteria":"Every indexable template overrides {% block description %} with a templated, page-specific description.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:11Z","created_by":"Ross Karchner","updated_at":"2026-08-13T21:56:26Z","started_at":"2026-08-13T21:54:36Z","closed_at":"2026-08-13T21:56:26Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.3","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:11Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.2","title":"SEO: fix duplicate Open Graph tags on category and location pages","description":"category_page.html:27-31 and location_page.html:20-24 emit a second og:title/og:type/og:description/og:site_name in extra_head, after base.html:27-31 already emitted them. Scrapers take the first occurrence, so the page-specific values are dead code.","acceptance_criteria":"Each page emits exactly one of each og:* tag, carrying the page-specific value. Overrides use the existing {% block og_title %}/{% block og_description %} blocks.","status":"closed","priority":1,"issue_type":"bug","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:10Z","created_by":"Ross Karchner","updated_at":"2026-08-13T21:56:26Z","started_at":"2026-08-13T21:54:26Z","closed_at":"2026-08-13T21:56:26Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.2","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:09Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.1","title":"SEO: add Sitemap directive to robots.txt","description":"site/static/robots.txt lists Disallow rules but never points crawlers at the sitemap.","acceptance_criteria":"robots.txt contains 'Sitemap: https://dctech.events/sitemap.xml'.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:09Z","created_by":"Ross Karchner","updated_at":"2026-08-13T21:56:26Z","started_at":"2026-08-13T21:54:05Z","closed_at":"2026-08-13T21:56:26Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.1","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:08Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j","title":"SEO improvements for dctech.events","description":"Ten SEO improvements identified in a 2026-08-13 audit of site/templates and packages/calgen. Ordered roughly by impact. Constraint that shapes the whole epic: the site sends users to primary sources immediately and must not become a 'roach motel' calendar — outbound links stay primary everywhere.","acceptance_criteria":"All ten child issues closed; site builds and freezes cleanly.","status":"closed","priority":1,"issue_type":"epic","owner":"ross@karchner.com","created_at":"2026-08-13T21:51:44Z","created_by":"Ross Karchner","updated_at":"2026-08-14T02:26:06Z","closed_at":"2026-08-14T02:26:06Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-hiy.1","title":"Rewrite updates_publisher to snapshot newly added events","description":"Select events by EVENT# createdAt within [publish_date-7d, publish_date), joined against the site's published events.json so the post can only ever list events the calendar is actually showing. Key the item UPDATE#\u003cpublish-date\u003e.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-11T00:07:07Z","created_by":"Ross Karchner","updated_at":"2026-08-11T00:20:40Z","started_at":"2026-08-11T00:07:16Z","closed_at":"2026-08-11T00:20:40Z","close_reason":"Implemented and unit-tested; verified end to end against live data with a local calgen build (23 events, /updates/2026/8/12/ renders).","dependencies":[{"issue_id":"next_dctech_events-hiy.1","depends_on_id":"next_dctech_events-hiy","type":"parent-child","created_at":"2026-08-10T20:07:06Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-hiy.2","title":"Move the publish schedule to Wednesday and grant table reads","description":"cron(0 11 ? * WED *) instead of MON, and grant_read_write_data so the publisher can scan EVENT# createdAt.","notes":"Code change done (cron WED + grant_read_write_data). cdk deploy NextUpdatesStack was blocked by the permission classifier and needs the user to run or approve it.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-11T00:07:07Z","created_by":"Ross Karchner","updated_at":"2026-08-11T01:32:32Z","closed_at":"2026-08-11T01:32:32Z","close_reason":"Deployed: rule cron(0 11 ? * WED *) ENABLED, publisher has table read+write.","dependencies":[{"issue_id":"next_dctech_events-hiy.2","depends_on_id":"next_dctech_events-hiy","type":"parent-child","created_at":"2026-08-10T20:07:07Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-hiy","title":"Weekly /updates post: newly added events, published Wednesdays","description":"Today the updates publisher runs Monday and snapshots the events HAPPENING that ISO week. The desired product is different: every Wednesday, /updates gets a post listing every event ADDED to the calendar in the past seven days, excluding anything still sitting in the moderation queue.\n\nRequires: new selection logic (join the published events.json against EVENT# createdAt), a new item key (UPDATE#\u003cpublish-date\u003e, since UPDATE#\u003ciso-week\u003e already has this week's Monday post), a Wednesday schedule, DynamoDB read permission for the publisher, and calgen rendering the new post shape.","acceptance_criteria":"A Wednesday-scheduled Lambda writes UPDATE#YYYY-MM-DD containing only events added in the previous 7 days that are live on the site; /updates/\u003cy\u003e/\u003cm\u003e/\u003cd\u003e/ renders it; pending submissions never appear.","notes":"Deployed 2026-08-10. NextUpdatesStack + NextSiteGeneratorStack are live; site rebuilt. Verified in production: the deployed Lambda dry-runs to 23 events for the 2026-08-12 window, the schedule rule is cron(0 11 ? * WED *) ENABLED, and the existing Monday post still renders. First real post lands Wednesday 2026-08-12 11:00 UTC — leaving it to the schedule rather than publishing early, since a post written today would be missing everything added on the 10th and 11th and the scheduled run would then skip the week.","status":"closed","priority":1,"issue_type":"feature","owner":"ross@karchner.com","created_at":"2026-08-11T00:06:53Z","created_by":"Ross Karchner","updated_at":"2026-08-14T02:15:26Z","closed_at":"2026-08-14T02:15:26Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-cpp.4","title":"QC: AgentCore CfnRuntime stack + weekly trigger","description":"Rewrite stacks/qa_agent_stack.py: CfnRuntime (CodeConfiguration/PYTHON_3_12, S3 asset, PUBLIC, HTTP), execution role (bedrock InvokeModel, execute-api:Invoke on MCP, agentcore browser, ses), qa_trigger Lambda on weekly EventBridge rule. Expose mcp_url on NextApiStack and wire through app.py. Gotchas: ARM64/Graviton wheels, AgentRuntimeName allows no hyphens, ping thread must not block.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-08T22:19:01Z","created_by":"Ross Karchner","updated_at":"2026-08-08T22:34:04Z","closed_at":"2026-08-08T22:34:04Z","close_reason":"Rewrote qa_agent_stack.py: CfnRuntime (PYTHON_3_12 CodeZip from an S3 asset, PUBLIC/HTTP), execution role (bedrock, execute-api on /mcp, agentcore browser, ses, logs), qa_trigger Lambda on a Monday 09:00 UTC rule minting the run id. Exposed mcp_url on NextApiStack. Removed the dead qa_agent stub. All 14 stacks synth.","dependencies":[{"issue_id":"next_dctech_events-cpp.4","depends_on_id":"next_dctech_events-cpp","type":"parent-child","created_at":"2026-08-08T18:19:01Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-cpp.3","title":"QC: calendar_qc agent source","description":"cdk_next/agents/calendar_qc/: main.py (BedrockAgentCoreApp entrypoint), mcp_sigv4.py (SigV4 streamable-HTTP transport, logic from scripts/mcp_sigv4_bridge.py), prompt.py (ported from calendar-qc.md), digest.py (SES digest, pattern from routes/submit.py:432-470). Queue = pending_qa; corpus = get_events(date_from=today) so cross-run duplicates are caught.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T22:19:00Z","created_by":"Ross Karchner","updated_at":"2026-08-08T22:27:39Z","started_at":"2026-08-08T22:22:36Z","closed_at":"2026-08-08T22:27:39Z","close_reason":"Wrote agents/calendar_qc/: main.py (two-pass orchestration + AgentCore entrypoint), mcp_sigv4.py (httpx SigV4 auth), prompt.py (ported QC rules, split triage/polish), digest.py (SES digest with undo instructions). Added list_pending_qa + resolve_qa_review MCP tools the agent needed. 18 plumbing tests. Fixed a _tool_result bug that silently emptied the work queue.","dependencies":[{"issue_id":"next_dctech_events-cpp.3","depends_on_id":"next_dctech_events-cpp","type":"parent-child","created_at":"2026-08-08T18:19:00Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-cpp.2","title":"QC: MCP tools — get_event, list_qa_run, revert_qa_run","description":"get_event(guid): full record for one event; get_events' compact projection omits description, which duplicate detection and category assignment both need. list_qa_run/revert_qa_run: undo path keyed on the '[qc:{run_id}]' stamp in the existing _comment overlay field.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T22:18:59Z","created_by":"Ross Karchner","updated_at":"2026-08-08T22:22:36Z","started_at":"2026-08-08T22:19:12Z","closed_at":"2026-08-08T22:22:36Z","close_reason":"Added get_event, list_qa_run, revert_qa_run; set_overlay takes run_id and snapshots prior values so revert restores rather than deletes. Generalized export to strip all underscore-prefixed overlay keys. 13 new tests, 35 pass in mcp/.","dependencies":[{"issue_id":"next_dctech_events-cpp.2","depends_on_id":"next_dctech_events-cpp","type":"parent-child","created_at":"2026-08-08T18:18:59Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-cpp.1","title":"QC: aggregator marks new iCal events pending_qa","description":"ical_aggregator/handler.py:135 — new iCal events get review_status='pending_qa' instead of 'approved'. Refresh path (line 129) already preserves review_status, so only genuinely new events are affected. Verified safe: db.get_all_events queries GSI4 and never filters review_status, so events stay visible while awaiting QC.","status":"closed","priority":1,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T22:18:58Z","created_by":"Ross Karchner","updated_at":"2026-08-08T22:22:34Z","started_at":"2026-08-08T22:19:12Z","closed_at":"2026-08-08T22:22:34Z","close_reason":"New iCal events now land in pending_qa; refresh preserves existing status. Verified get_all_events (GSI4) ignores review_status, so this doesn't gate publication.","dependencies":[{"issue_id":"next_dctech_events-cpp.1","depends_on_id":"next_dctech_events-cpp","type":"parent-child","created_at":"2026-08-08T18:18:58Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-cpp","title":"Calendar QC agent on AgentCore Runtime","description":"\n\nDONE 2026-08-27. The remaining item — deploy NextQaAgentStack so the runtime\nmatches HEAD — is done, along with rather more than that.\n\nThe runtime now carries a *reinstated* polish pass, which this bead had\nrecorded as removed. It was re-added deliberately: the original was cut for\nproducing zero overlays across four runs, but it had run on a cheaper model\nwith only the browser to read pages. It now runs on the same Sonnet 5 as triage\nand reads pages with tavily_extract. A dry run on 2026-08-26 proposed 26\nchanges where the old pass proposed none — five bare Luma URLs resolved to a\nreal venue, six Rust DC occurrences given the category they always wanted, and\na Qualtrics form it declined to guess at. Titles were left alone in all 25\ncorrections, and no category slug was invented.\n\nAlso changed since this bead was written:\n  * The weekly trigger is gone. QC is step 2 of NextOrchestrationStack's Monday\n    state machine, so the pass finishes before the site rebuild and week-ahead\n    post that depend on it. Watch the first live run — next_dctech_events-4rd.\n  * The trigger Lambda no longer blocks on InvokeAgentRuntime, and takes no\n    async retries. It had always died at its 60s timeout, and Lambda's default\n    two retries turned one pass into three concurrent ones that starved each\n    other of Bedrock throughput.\n  * The agent releases a Step Functions task token itself, because only it\n    knows when a pass is really over.\n  * Overlay writes go through db.set_event_overlay, shared with the /edit UI.\n\nClosing this; cpp.6 stays open on its own.","notes":"Deployed NextApiStack + NextQaAgentStack 2026-08-08. Runtime ARN: arn:aws:bedrock-agentcore:us-east-1:797438674243:runtime/dctechEventsCalendarQc-RoDU4O5Z9k. Live-verified: SigV4 auth works, 30 tools exposed, all 5 new tools present, corpus reads 199 future events. BLOCKED on the real dry run: NextIcalAggregatorStack is NOT yet deployed, so pending_qa is empty and nothing can enter the queue. Fixed three _tool_result bugs found only by hitting the live endpoint (MCPToolResult is a dict subclass; FastMCP emits one content block per list element; N=1 list vs scalar ambiguity).\nThree dry runs against 20 backfilled events. Triage (Sonnet 5) stable: same 2 duplicate pairs + 2 out-of-area (AFCEA Hampton Roads/Norfolk) every run, 0 false positives. Fixed: max_tokens unset (Bedrock default killed runs mid-pass); browser navigate reports a 30s timeout on Meetup while the page HAS loaded (prompt now says retry get_text); duplicate canonical direction flipped between runs 1 and 2 (prompt now ranks organiser over aggregator explicitly, run 3 confirms). Digest is now derived from list_qa_run rather than the models' self-report, because the polish pass writes overlays and then fails to emit parseable JSON. NOT yet done: live run.\nLive run done 2026-08-08 (run qc-2026-08-08-6fe45fba): 4 overlays written, verified on the rendered site — both AFCEA Hampton Roads events absent, both duplicate pairs collapsed to the organiser's listing, no _qa_run/run-id leakage in the HTML, queue drained 20 -\u003e 0, digest sent, CodeBuild SUCCEEDED. Polish pass then REMOVED (zero overlays across 4 runs). Committed + pushed as 919eef4. REMAINING: deploy NextQaAgentStack so the runtime matches HEAD — the deployed asset still carries the polish pass.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-08T22:18:40Z","created_by":"Ross Karchner","updated_at":"2026-08-27T19:47:19Z","closed_at":"2026-08-27T19:47:19Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-s2t","title":"Close public Cognito sign-ups","description":"Set AllowAdminCreateUserOnly=true on the shared user pool so the SignUp endpoint is closed and only AdminCreateUser can make accounts.\n\nSafe to change: it is a mutable UserPool property, unlike Schema/UsernameAttributes, so it does not force replacement of the pool holding 66 real accounts. Existing users keep signing in.\n\nMade possible by the magic-link submission flow — a self-service account no longer buys a submitter anything, and the open endpoint was absorbing sustained bot registrations.","acceptance_criteria":"Public sign-up via hosted UI or the SignUp API is rejected; existing users can still sign in; admins can still be created with AdminCreateUser.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-08T13:02:53Z","created_by":"Ross Karchner","updated_at":"2026-08-08T13:14:37Z","closed_at":"2026-08-08T13:14:37Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-qrt","title":"Magic-link event submission (drop Cognito requirement)","description":"Submitting an event currently requires a Cognito account, which is a large barrier for a one-off submitter. Replace it with an emailed magic link: the user enters an email, receives a signed link, and can submit without ever creating an account.\n\nToken is a KMS HMAC over email+timestamp, matching the newsletter confirmation-link precedent in NextNewsletterStack — stateless, so no session table or cleanup. /api/submissions and the new /api/submit-link drop the API Gateway Cognito authorizer and validate the token in Lambda instead; Cognito claims still work so admins and existing signed-in users are unaffected.\n\nBecause /api/submit-link sends mail to an arbitrary address, it needs a per-email cooldown or it is an open relay for filling someone's inbox.","acceptance_criteria":"A signed-out user can request a link, receive the email, click it, and submit an event that lands in the moderation queue with their email as submitter; expired/tampered tokens are rejected; repeated link requests for one address are rate-limited; admin flows still work via Cognito.","status":"closed","priority":1,"issue_type":"feature","owner":"ross@karchner.com","created_at":"2026-08-08T12:59:56Z","created_by":"Ross Karchner","updated_at":"2026-08-08T13:15:04Z","closed_at":"2026-08-08T13:15:04Z","close_reason":"Implemented and deployed; verified end-to-end in production","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-gst","title":"Fix invalid JSON-LD escaping in event templates","description":"Google Search Console reports 'unparseable structured data, Bad escape sequence in string'. The ItemList JSON-LD blocks use a hand-rolled {{ x | replace('\"', '\\\\\"') }} escaper that fights Jinja autoescape: the inserted quote becomes \u0026#34;, leaving \\\u0026#34; (invalid JSON escape). Titles with \u0026 also render as literal \u0026amp; in the ingested value. Fix by using the tojson filter, which emits its own quotes, escapes correctly, and is script-safe.","notes":"Fix committed as 3d80077 (site/templates/homepage.html, virtual_events.html: tojson). Verified by rendering both templates with adversarial titles — all 4 ld+json blocks parse, values round-trip exactly. cdk_next/build/site_src rebuilt via build_lambdas.sh; cdk diff clean (only S3 asset hash). BLOCKED on 'cdk deploy NextSiteGeneratorStack --exclusively' (denied by permission classifier) — CodeBuild source is a CDK S3 asset, so start-build alone rebuilds from stale templates. Note: CDK CLI must be npx -y aws-cdk@latest; the cached 2.1119.0 rejects schema 54.","status":"closed","priority":1,"issue_type":"bug","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-06T10:24:27Z","created_by":"Ross Karchner","updated_at":"2026-08-06T12:06:39Z","started_at":"2026-08-06T10:24:51Z","closed_at":"2026-08-06T12:06:39Z","close_reason":"Deployed and verified live 2026-08-06. Build 60354c57 SUCCEEDED, CloudFront invalidation completed; all 4 ld+json blocks on / , /virtual/ and www parse, and the previously-broken title round-trips as '\"Am I?\" Film Screening \u0026 Discussion'.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-b07","title":"build_lambdas.sh shipped stale calgen from uv cache","description":"calgen's version is pinned at 0.1.0 and never changes, so uv treated the locally-built wheel as cache-valid and reused it even after packages/calgen source changed. Lambda bundles and the CodeBuild wheel silently shipped pre-edit code.\n\nCaught while working next_dctech_events-7lg: the deployed iCal aggregator contained zero references to HORIZON_DAYS after a clean build_lambdas.sh run and deploy, so the 180-day horizon had no effect in production despite the source being correct and passing local tests (the local venv uses an editable install, which reads source directly and therefore masked the problem).\n\nFixed by forcing a rebuild in all three places calgen is packaged:\n  CALGEN_FRESH=(--refresh-package calgen --reinstall-package calgen)  # both uv pip install targets\n  uv build --no-cache ...                                            # CodeBuild wheel\n\n## Context\nWorth remembering: an editable install in cdk_next/.venv means local pytest and local calgen builds always see current source, so this class of bug cannot be caught locally — only by inspecting the built artifact or the deployed function.","acceptance_criteria":"After editing packages/calgen source, ./build_lambdas.sh produces bundles and a wheel containing the edit. Verified by grepping build/ical_aggregator/calgen, build/newsletter/calgen, and the built wheel.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T17:25:30Z","created_by":"Ross Karchner","updated_at":"2026-08-02T17:25:31Z","closed_at":"2026-08-02T17:25:31Z","labels":["build","fixed"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-udg","title":"db.py drops skip_phrases when reading GROUP items","description":"_group_item_to_dict in cdk_next/lambda_src/api/db.py copies a fixed allowlist of fields off the GROUP# item:\n\n    for field in ['website', 'ical', 'fallback_url', 'categories',\n                  'suppress_urls', 'suppress_guid', 'scan_for_metadata', 'url_override']:\n\nskip_phrases is absent, so the field is silently discarded on every read even though put_group writes it and the migration carried it over from _groups/*.yaml (nvtc.yaml sets it). Anything reading groups through this helper — the site-generator export, the iCal aggregator, and the MCP list_groups tool — never sees it.\n\n## Context\nIntroduced when backend/db.py was forked for this stack. One-line fix, but it blocks any skip_phrases support in calgen from taking effect.","acceptance_criteria":"skip_phrases survives a get_group/get_all_groups round-trip and reaches _groups/*.yaml via export_dynamo_to_calgen.py.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:39:00Z","created_by":"Ross Karchner","updated_at":"2026-08-02T17:14:17Z","started_at":"2026-08-02T17:13:49Z","closed_at":"2026-08-02T17:14:17Z","labels":["api","bug","dynamodb"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-7lg","title":"Cap iCal fetch horizon at 180 days","description":"The vendored calgen has no upper bound on how far ahead it keeps iCal events, so dctech.events currently lists events out to 2029. The pre-cutover site capped them at 180 days.\n\nIn packages/calgen/src/calgen/calendars.py, fetch_ical_and_extract_events expands each VEVENT with an rrule individually over a 90-day window (recurring_end_dt) but applies no horizon at all to non-recurring events. Upstream replaced this with a single whole-calendar expansion bounded at 180 days:\n\n    start_dt = datetime.now(timezone.utc)\n    end_dt = start_dt + timedelta(days=180)\n    for event in recurring_ical_events.of(cal).between(start_dt, end_dt):\n\nMeasured against the live AFCEA feed: the current code yields 76 events through 2027-06, the 180-day form yields 39 through 2027-01.\n\n## Context\nBehaviour exists in the now-archived rosskarchner/calgen at origin/main (commit ce08c53 lineage), fetchable from the local checkout at ~/projects/calgen. Never ported when calgen was vendored, because the vendored copy came from a local working tree that predated it.","acceptance_criteria":"Events beyond 180 days no longer appear on dctech.events. The iCal aggregator's prune step removes the now-out-of-horizon EVENT# items on its next run (expect roughly 96 removals). Site build succeeds and the homepage/month pages still render.","status":"closed","priority":1,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:38:25Z","created_by":"Ross Karchner","updated_at":"2026-08-02T17:15:09Z","started_at":"2026-08-02T17:14:17Z","closed_at":"2026-08-02T17:15:09Z","labels":["calgen","ical","site-content"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-3zz","title":"Right-size Bedrock model tier for the QC agent's triage step","description":"cdk_next/agents/calendar_qc/main.py defaults QC_TRIAGE_MODEL to claude-sonnet-5, including the triage pass, which is typically simple pattern-matching rather than deep reasoning -- POLISH_PROMPT's harder judgment calls are a separate step. Given the documented ~$250 runaway-cost incident, model tier is the dominant cost lever whenever this agent runs. Note: this item originally also covered the discovery agent's model choice, moot since that agent was deleted 2026-09-01. Fix: use a Haiku-class model for triage, keep Sonnet only for the polish/judgment pass.","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:32Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:32Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-3ce","title":"Document/monitor the magic-link KMS signing key","description":"magic_link.py's SUBMIT_KEY_ID is the sole authentication factor for anonymous submission/correction, and this session discovered the memory-recorded key ID was stale -- meaning key rotation/replacement isn't tracked anywhere reviewable. Fix: tag/document the active key ID in a CDK output; alarm on kms:GenerateMac from unexpected principals.","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:16Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:16Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-iw0","title":"Rate-limit the public submission/correction write endpoints, not just link issuance","description":"check_and_record_link_request throttles magic-link requests, but /api/corrections and /api/submissions themselves have no per-IP or per-submitter rate limit once a token is verified -- a leaked or replayed 24-hour magic-link token could be used to spam corrections/submissions against many events in quick succession. Fix: add a coarse per-submitter-id rate limit on the write path itself.","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:15Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:15Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-uaa","title":"Audit is_safe_url is applied at every URL-accepting write path","description":"update_event's url field does call is_safe_url, but the overlay/correction url writers added this session (set_event_overlay, the new corrections feature) inherited the pattern by convention rather than through a shared, enforced validator. Worth an explicit audit pass confirming every url-accepting write path (overlay, correction, draft promotion) routes through the same check before storage, not just before render.","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:15Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:15Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-0ka","title":"Scope QC agent's Bedrock InvokeModel grant off resources=[\"*\"]","description":"cdk_next/stacks/qa_agent_stack.py grants bedrock:InvokeModel(WithResponseStream) on resources=[\"*\"] -- every model in the account/region, not just the ones this agent is meant to use (partly justified by cross-region inference profiles, but wider than necessary). A compromised agent (real attack surface: it reads scraped web content) could run up cost on arbitrary models. Note: this item originally also covered the discovery agent's identical grant, which was removed with that stack's deletion on 2026-09-01 -- this is the remaining scope. Fix: scope to the specific inference-profile/model ARN prefixes actually in use.","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:04Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:04Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-3sb","title":"Scope overly broad SES send grants to specific identities","description":"cdk_next/stacks/api_stack.py and newsletter_stack.py grant ses:SendEmail (plus contact-management actions) with resources=[\"*\"] -- any verified SES identity in the account, not scoped to outbound@dctech.events / newsletter@dctech.events. A bug that let an attacker control FromEmailAddress could spoof mail from any other verified sender in the account. Fix: scope to the specific identity ARN(s).","status":"open","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:03Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:03Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-0zc","title":"Fix: signed-in users get 401 on /api/submissions and /api/corrections","description":"Fixes next_dctech_events-1k7. get_user_from_event now falls back to manually verifying the Authorization Bearer ID token against the Cognito JWKS (pyjwt PyJWKClient, cached module-level) when requestContext.authorizer.claims is empty -- the case on gateway-unauthenticated routes like /api/submissions and /api/corrections. Checks signature (RS256), exp/iat/sub required, audience=COGNITO_USER_POOL_CLIENT_ID, issuer derived from COGNITO_USER_POOL_ID; only accepts token_use=id (matches what DctechAuth.authorizedFetch sends). Added pyjwt[crypto] to the api lambda's build_lambdas.sh deps. 13 new tests in test_auth.py using a real locally-generated RSA keypair (fakes only the JWKS network fetch, real signature/exp/aud/iss verification). Full suite: 338 passing.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T12:31:53Z","created_by":"Ross Karchner","updated_at":"2026-09-01T12:32:00Z","closed_at":"2026-09-01T12:32:00Z","close_reason":"Implemented, tested (13 new unit tests, 338 passing overall). Deploying and E2E-verifying next.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-1k7","title":"Signed-in (non-magic-link) users get 401 on /api/submissions and /api/corrections","description":"get_user_from_event() only reads requestContext.authorizer.claims, which API Gateway populates ONLY when a route has the Cognito authorizer attached. /api/submissions and /api/corrections are deliberately unauthenticated at the gateway (so magic-link-only requests reach the Lambda), so a signed-in Cognito user with no magic link hits _resolve_submitter's Cognito fallback, which always 401s ('Please request a submission link, or sign in, to submit.') even with a valid Authorization: Bearer \u003cidToken\u003e header. Confirmed on live prod via curl against /api/submissions (pre-existing, not introduced by the corrections feature) and via Playwright against the new /api/corrections. Fix requires get_user_from_event to fall back to manually verifying the Bearer JWT against the Cognito JWKS (pyjwt + PyJWKClient) when authorizer.claims is absent -- real security-sensitive code deserving its own careful implementation and tests, not a quick patch. Affects: frontends/edit/js/submission.js and js/correction.js's signed-in (non-magic-link) branch.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T12:09:41Z","created_by":"Ross Karchner","updated_at":"2026-09-01T12:31:59Z","closed_at":"2026-09-01T12:31:59Z","close_reason":"Fixed by next_dctech_events-0zc: get_user_from_event now verifies the Authorization Bearer token against the Cognito JWKS when the gateway authorizer provided no claims.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-0yi","title":"Broaden Meetup JSON-LD @type check beyond literal 'Event'","description":"fetch_json_ld_data (packages/calgen/src/calgen/calendars.py) only matched item['@type'] == 'Event', so Meetup pages that emit a more specific schema.org subtype (ScreeningEvent, BusinessEvent, etc.) never had their eventAttendanceMode/location read. Root cause of nova-scribes 'The Art of the Chart' not being flagged as an online event. Fixed by matching against an explicit set of schema.org Event subtypes.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-29T02:15:15Z","created_by":"Ross Karchner","updated_at":"2026-08-29T02:15:35Z","closed_at":"2026-08-29T02:15:35Z","close_reason":"Fixed in 57b2318: matched item['@type'] against an explicit set of schema.org Event subtypes instead of exact-equality to 'Event'; added regression tests.","labels":["bug","calgen"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-abo","title":"list_pending_qa limit is silently ignored","description":"`list_pending_qa(limit=N)` returns every pending event regardless of N, so\nthere is no way to run a bounded QC pass. Found while dry-running the QC agent\non 2026-08-26: invoked with {\"limit\": 10}, the agent logged \"36 events pending\nQC\" and reviewed all 36.\n\nCAUSE (verified): db.get_events_by_review_status (cdk_next/lambda_src/api/db.py\n:463-473) does\n\n    if limit:\n        kwargs['Limit'] = limit\n    items = _query_all(table, **kwargs)\n\nDynamoDB's `Limit` caps items *per page*, and `_query_all` (db.py:56-62) then\nfollows LastEvaluatedKey until the query is exhausted. So Limit=10 means \"10\nper page, fetch every page\" — the opposite of what the caller asked for, and\nit fails silently.\n\nWHY IT MATTERS: `limit` exists so a new prompt can be evaluated against a\nhandful of events instead of the whole queue. Without it, every dry run is a\nfull-queue pass — slower, more expensive, and more likely to hit the Bedrock\nread timeouts that killed the 2026-08-26 dry run. It is the difference between\na two-minute test and a fifteen-minute one.\n\nFIX: truncate after paginating, or stop paginating once enough rows are in\nhand. Slicing the result is the smaller change and keeps `_query_all` general:\n\n    items = _query_all(table, **kwargs)\n    if limit:\n        items = items[:limit]\n\nLeaving `Limit` in kwargs is still worth it — it keeps the per-page read cost\ndown even though it does not bound the total.\n\nCHECK THE OTHER CALLERS before changing this: get_events_by_review_status is\nshared by the MCP server, the API and the discovery agent's proposal queue, so\nconfirm none of them are relying on the current all-rows behaviour while\npassing a limit. Grep for get_events_by_review_status and for the MCP tools\nlist_pending_qa / list_discovery_proposals.\n\nSame shape may exist elsewhere — audit other `kwargs['Limit']` uses next to\n_query_all / _scan_all in db.py.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-26T14:45:19Z","created_by":"Ross Karchner","updated_at":"2026-08-27T18:23:30Z","closed_at":"2026-08-27T18:23:30Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-8so","title":"Normalize doubled 'City, ST' in iCal event locations","description":"Many EVENT# locations end with a repeated `City, ST`, so listings display a\ncity and state twice.\n\nObserved in the pending QC queue (2026-08-26):\n\n  a82752cd… \"Rockville Memorial Library, Rockville Town Square Plaza,\n             21 Maryland Ave, Rockville, MD, Rockville, MD\"\n  8ea07ae7… \"Fuse At Mason Square, 3351 Fairfax Drive,, Arlington, VA,\n             Arlington, VA\"\n  13a742bc… \"Barbara M. Donnellan Auditorium, 1015 N Quincy St.,\n             Arlington, va, Arlington, va\"      \u003c- lowercase state\n  40f001f0… \"HacDC, 3333 14th St NW, Suite M110, Washington, DC,\n             Washington, DC\"\n\nSpans many groups, which is what makes it a pipeline issue rather than\norganiser typos.\n\nCAUSE (verified): Meetup's own iCal LOCATION property already ends in\n`City, ST`, and Meetup appends city and state again. calgen passes the value\nthrough verbatim — packages/calgen/src/calgen/calendars.py:206-207,\n`resolved_location = ical_location`.\n\nNOT the cause: the JSON-LD address join at calendars.py:96-101. That branch\nonly fires when the iCal LOCATION is empty (see the `if not ical_location`\nguard at :216), so it is not involved in these cases.\n\nBLAST RADIUS is display only — I checked. `location_utils.extract_location_info`\nreads from the end of the string and normalizes case, so region faceting and\n/locations/ are unaffected:\n  extract_location_info(\"… Arlington, va, Arlington, va\") -\u003e ('Arlington', 'VA')\nPurely cosmetic, hence P2 rather than higher.\n\nFIX DIRECTION: normalize once at parse time in calendars.py, before the value\nreaches `event_data`. Collapse a trailing repeat of the final `City, ST`, and\nwhile there, squeeze the doubled commas Meetup also emits (\"3351 Fairfax\nDrive,,\"). Doing it at parse time means the stored value is clean and no\nconsumer has to defend against it.\n\nWHY IT IS WORTH DOING even though it is cosmetic: the QC polish pass\n(next_dctech_events-wm4 follow-on) is now pointed at exactly this kind of\nlocation defect. A one-time normalizer removes the work permanently; leaving it\nmeans paying a model call every week to correct machine-generated noise.\n\nSEPARATE PROBLEM, do not conflate: 13a742bc's sibling 8ea07ae7/dd0d5c82\n(\"DevFest DC 2026\") carries \"Arlington, DC\" — Arlington is in Virginia. That is\nMeetup's own wrong addressRegion, so normalization cannot fix it; it currently\nfiles the event under Washington DC. Needs the polish pass or a human.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-26T14:31:37Z","created_by":"Ross Karchner","updated_at":"2026-08-27T20:04:29Z","closed_at":"2026-08-27T20:04:29Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-o1e","title":"Set up Agent Toolkit for AWS","description":"Install and configure Agent Toolkit for AWS from upstream setup instructions, and document AWS-native alternative to Tavily for discovery search.","status":"closed","priority":2,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-24T12:29:27Z","created_by":"Ross Karchner","updated_at":"2026-08-24T12:34:46Z","started_at":"2026-08-24T12:29:33Z","closed_at":"2026-08-24T12:34:46Z","close_reason":"Agent Toolkit installed with default profile, aws-mcp profile env propagated to configs, skill catalog verified, and AWS rules added to AGENTS/CLAUDE.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-lux","title":"Overlay edits should trigger site rebuilds","description":"CORRECTED 2026-08-27. The original diagnosis was wrong, but the reported\nsymptom is real and still unfixed.\n\nWRONG: \"Overlay writes use a different key prefix and are therefore invisible\nto the trigger.\" There is no OVERLAY# prefix. An overlay is the `overrides` map\n*on the EVENT# item* — set_overlay calls db.update_event, which updates\nEVENT#{guid}/META — and EVENT# is already in RELEVANT_PREFIXES. So overlay\nwrites have always been visible to the trigger. The suggested fix (add OVERLAY#\nto RELEVANT_PREFIXES) would be a no-op.\n\nProved in production on 2026-08-27. Two overlay writes at 15:34:58Z, then the\ntrigger's own log:\n\n  15:35:41  Build already in progress; skipping (scheduled build will catch up)\n  15:36:27  Build already in progress; skipping (scheduled build will catch up)\n  15:41:33  Started build ... (2 relevant changes)\n\nREAL CAUSE: the trigger drops work when a build is already running —\ncdk_next/lambda_src/site_generator/trigger/handler.py, the\n`if any(b['buildStatus'] == 'IN_PROGRESS')` branch. Its comment says \"the\nscheduled build will catch up\", but the safety-net build is daily at 09:00 UTC,\nso a hide applied at the wrong moment can sit unpublished for up to 24 hours.\nThat is exactly what happened when AFCEA Belvoir was hidden by hand and\nCodeBuild had to be force-invoked.\n\nTHE FIX IS NOW CHEAPER THAN IT WAS. That skip existed to stop two builds\nrunning `s3 sync --delete` over the same bucket concurrently. As of the Monday\nstate machine work the project carries concurrent_build_limit=1, so CodeBuild\n*queues* a second start_build instead of running it — verified:\n  aws codebuild batch-get-projects --names dctech-events-next-site-generator \\\n    --query 'projects[0].concurrentBuildLimit'  -\u003e  1\n\nSo the skip is obsolete. Delete the in-progress check and call start_build\nunconditionally; CodeBuild serialises. Points to watch when doing it:\n  * A burst of stream batches would queue a build each. The event source\n    mapping already batches on a 60s window, but consider coalescing — or\n    accept the queue, since a queued build is cheap and idempotent.\n  * Check the queue timeout: a build queued behind a long one can expire.\n  * The Monday state machine's two codebuild:startBuild.sync steps will now\n    sometimes wait through a queued trigger build. BUILD_TIMEOUT is 30 min in\n    orchestration_stack.py, which covers it, but that is the number to revisit\n    if it ever bites.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-24T12:26:16Z","created_by":"Ross Karchner","updated_at":"2026-08-27T19:51:33Z","closed_at":"2026-08-27T19:51:33Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-kub","title":"Set production Tavily key for discovery agent","description":"Discovery runtime secret dctech-events-next/discovery/tavily currently contains a placeholder/generated value. Set a valid Tavily API key so open-web search tools succeed (tavily_search/tavily_extract currently return Unauthorized in runtime logs).","status":"in_progress","priority":2,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-24T12:25:28Z","created_by":"Ross Karchner","updated_at":"2026-08-24T14:05:18Z","started_at":"2026-08-24T14:05:18Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-kqt","title":"Handle cancelled events from iCal feeds","description":"The iCal ingestion pipeline currently imports events even when they are cancelled. Cancellation can appear in two ways:\n\n1. iCal standard: STATUS:CANCELLED property on the VEVENT\n2. Source-specific: title contains '*** Cancelled ***' (seen with AFCEA Belvoir Eventbrite feed)\n\nThe QA agent should detect events with these signals and either skip them during ingestion or flag/hide them automatically.\n\nExample: AFCEA Belvoir August 2026 Luncheon *** Cancelled *** (guid: 653c72128c89e69b95fb01687e369936) — was manually hidden via overlay on 2026-08-24.\n\nSuggested approach:\n- During iCal processing, check VEVENT STATUS:CANCELLED and skip those events\n- Also filter titles matching case-insensitive pattern like '\\*\\*\\* cancel' \n- For already-imported cancelled events, the QA agent should identify and hide them via overlay","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-24T12:20:57Z","created_by":"Ross Karchner","updated_at":"2026-08-27T20:08:31Z","closed_at":"2026-08-27T20:08:31Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.10","title":"SEO: per-event landing pages that route users to the primary source","description":"Every event links straight out (events_by_day.html:21), so the site has no indexable URL for any individual event and cannot rank for long-tail queries like 'kubernetes meetup arlington'. The homepage ItemList JSON-LD (homepage.html:55-106) is also ineligible for Google event rich results: items lack their own url on this domain, a full PostalAddress, and endDate.","design":"HARD CONSTRAINT: this site sends users to primary sources immediately and must not become a 'roach motel' calendar. Design rules, all required:\n\n1. List pages keep linking directly out. events_by_day.html:21 is unchanged - the event TITLE always points at the organizer's URL, target=_blank.\n2. The event page is a SEARCH LANDING PAGE, not a step in the site's own click path. It serves people arriving from Google who had no direct path to intercept.\n3. Internal discovery is via a low-prominence permalink (a '#' glyph or the timestamp) beside the title, blog-permalink style. Crawlers follow it; users who want the event never click it. Orphan pages do not rank, so some internal link is required - the resolution is prominence, not presence.\n4. On the event page the primary source link is THE primary action: above the fold, largest affordance, organizer's real hostname visible. No redirect interstitial, no click wrapper, no signup wall - a direct href.\n5. Do NOT republish the organizer's description (available at calendars.py:231). That is both the substitution behavior we object to and duplicate content that would get the page filtered. Show facts only: when, where, who, categories.\n6. Earn the page's existence with information the source does not have: also_published_by (which other groups list this event), a per-event .ics download, category cross-links, and persistence after the source link rots. Pairs with the archive work in the sibling issue.\n7. Event JSON-LD: url points at our page (that is what earns the rich result); the source goes in offers.url, which is where Google routes 'register' clicks anyway. Include startDate, endDate, full PostalAddress, organizer, eventAttendanceMode, eventStatus.\n\nConsidered and rejected: anchor IDs on existing week/month pages. Honest and nearly free, but Google rarely ranks a fragment URL independently and per-event Event schema cannot attach cleanly to a 40-event page. Roughly a tenth of the upside.","acceptance_criteria":"/events/\u003cslug\u003e/ pages freeze for every current event, carry valid Event structured data, appear in the sitemap, and are reachable only via the low-prominence permalink. Outbound behavior on all list pages is byte-identical to before except for the added permalink. Event descriptions from feeds are not republished.","status":"closed","priority":2,"issue_type":"feature","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:55Z","created_by":"Ross Karchner","updated_at":"2026-08-13T22:07:01Z","started_at":"2026-08-13T22:03:22Z","closed_at":"2026-08-13T22:07:01Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.10","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:55Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.9","title":"SEO: keep past month and week pages instead of letting them 404","description":"get_upcoming_months() (app.py:629) derives months from current events only, and get_upcoming_weeks(12) bounds weeks the same way. Past month and week pages stop being generated and their URLs start 404ing, so accumulated links and rankings for e.g. /2026/6/ die on a monthly schedule.","acceptance_criteria":"Past month and week pages stay frozen as archives, appear in the sitemap with an honest lastmod and changefreq never, and are reachable by an internal link. No previously-published URL 404s after a rebuild.","notes":"Investigated; needs a design decision before implementation. Confirmed the harm is real: site_generator_stack.py:52 deploys with 'aws s3 sync build/ --delete', so a month page that stops being generated is actively deleted from the bucket and returns a hard 404.\n\nWhy this is not a small fix: there is no historical event data anywhere in the pipeline. pipeline.py:304 and :328 drop events with event_date \u003c today at ingest, and the ical cache only ever holds what the organizer's feed currently advertises - once an event passes, the source feed drops it and calgen has already discarded it. So past month/week pages cannot be rebuilt from anything on disk.\n\nThe weekly /updates roundups looked like an existing archive, but updates.py's module docstring rules that out going forward: as of 2026-08-12 roundups list the events ADDED that week, not the events HAPPENING that week. Only pre-2026-08-12 posts are 'what happened' snapshots.\n\nThe build is ephemeral CodeBuild (site_generator_stack.py BUILDSPEC) regenerating _data from a DynamoDB export each run, so any archive needs new persistent storage (S3 object or DynamoDB items) plus pipeline merge logic plus freezer generators plus a stack change for permissions.\n\nOptions put to the user 2026-08-13:\n  a) Real archives - persist past events to a new store; accumulates from the change forward, cannot recover history already lost.\n  b) Archive stubs - calgen enumerates every month/week from site launch to today and emits a 200 page saying the events have passed, linking to /updates and current listings. No new storage; deterministic; thin, so noindex+follow to preserve link flow without indexing near-duplicates.\n  c) Accept the 404s.\n\nRejected as unworkable: scoping the s3 sync --delete to spare past paths. --exclude applies to source and destination alike, so sparing /2026/8/ from deletion would also stop it updating while it is still the current month, and the pattern cannot distinguish past from current.\nIMPLEMENTED as variant A (publisher-side), per user 2026-08-13. The earlier 'needs new persistent storage' framing was wrong: _updates/ is already a DynamoDB export (export_dynamo_to_calgen.py:152), so the durable store already existed.\n\nPublisher (updates_publisher/app.py): added _next_week_bounds, _events_in_week, _archive_item. Each Wednesday run now also writes ARCHIVE#\u003cweek_id\u003e/SK=META for the FOLLOWING ISO week, computed from the events.json it already fetches - no new source, no new fetch, no IAM change. Guarded on today (not publish_date) so a backdated manual re-run cannot overwrite a good capture with an empty one.\nExport: ARCHIVE items land in _archive/{week_id}.yaml. by_prefix is generic over PK prefix and filters SK==META, so no scan change was needed.\ncalgen: new archive.py (get_archived_weeks/week/events, merge_events). Week and month routes merge live + archived, live winning on conflict. get_all_week_ids/get_all_months drive the freezer and sitemap so past pages keep building.\nTemplates: archive-notice on past week/month pages; permalinks withheld from archived events since /events/ pages exist only for current events.\nAlso fixed a pre-existing bug the archive exposed: month prev/next nav linked to months that were never frozen (has_prev/has_next now gate it).\n\nKnown limits, by design: coverage starts when archiving starts - months already lost stay lost; a capture is taken up to a week ahead, so events added after it are missing (same fidelity the weekly email has). Variant B (site generator refreshes archives every build) closes that gap but needs a table write permission on the CodeBuild role.","status":"closed","priority":2,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:54Z","created_by":"Ross Karchner","updated_at":"2026-08-13T22:24:23Z","started_at":"2026-08-13T22:01:41Z","closed_at":"2026-08-13T22:24:21Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.9","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:53Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.8","title":"SEO: noindex thin category-month facet pages","description":"The sidebar cross-links every category with every month and freeze.py:54 materializes each combo. Combos holding one or two events are thin near-duplicates competing with their parent /categories/\u003cslug\u003e/ page.","acceptance_criteria":"Category-month pages below an event threshold emit robots noindex,follow (or canonical to the parent category). Substantial ones are in the sitemap. Same treatment for zero-event location pages.","status":"closed","priority":2,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:31Z","created_by":"Ross Karchner","updated_at":"2026-08-13T22:01:40Z","started_at":"2026-08-13T22:00:54Z","closed_at":"2026-08-13T22:01:40Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.8","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:30Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u4j.7","title":"SEO: add og:image and favicon assets","description":"base.html:32-55 has all image/icon tags commented out pending a HEADER_IMAGES.md that does not exist in the repo. site/static/images/ holds only a README and a sizes reference. Every social share renders as a bare text link and Google's mobile results show a blank favicon.","acceptance_criteria":"A 1200x630 OG card and a favicon set exist under site/static/images/, the commented-out tags in base.html are live, and site/static/manifest.json.template is resolved.","status":"closed","priority":2,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-13T21:52:29Z","created_by":"Ross Karchner","updated_at":"2026-08-13T22:09:38Z","started_at":"2026-08-13T22:07:13Z","closed_at":"2026-08-13T22:09:38Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-u4j.7","depends_on_id":"next_dctech_events-u4j","type":"parent-child","created_at":"2026-08-13T17:52:29Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-p8o","title":"Submitted events get a different guid on the site than in DynamoDB","description":"db.put_event keys submitted events EVENT#\u003c8 hex\u003e but calgen's load_single_events unconditionally recomputes guid = calculate_event_hash(date,time,title,url) (pipeline.py:167), so events.json shows a 32-char guid for them. Consequences: _overlay/\u003cdynamo-guid\u003e.yaml never matches a submitted event, so overrides on submissions silently do nothing; and anything joining events.json back to the table by guid misses them (the updates publisher works around it with a date+title fallback). Fix is probably for load_single_events to honor a guid already present in the YAML and for the exporter to write it.","status":"closed","priority":2,"issue_type":"bug","owner":"ross@karchner.com","created_at":"2026-08-11T00:07:10Z","created_by":"Ross Karchner","updated_at":"2026-08-27T15:04:27Z","closed_at":"2026-08-27T15:04:27Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-hiy.3","title":"Render the new post shape in calgen","description":"updates.py: honor published_on and an explicit stored summary; update_post.html: do not label a newly-added roundup as 'this week'.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-11T00:07:09Z","created_by":"Ross Karchner","updated_at":"2026-08-11T00:20:41Z","closed_at":"2026-08-11T00:20:41Z","close_reason":"Implemented and unit-tested; verified end to end against live data with a local calgen build (23 events, /updates/2026/8/12/ renders).","dependencies":[{"issue_id":"next_dctech_events-hiy.3","depends_on_id":"next_dctech_events-hiy","type":"parent-child","created_at":"2026-08-10T20:07:08Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-cpp.5","title":"QC: build calendar_qc asset (ARM64)","description":"build_lambdas.sh: replace the qa_agent stub arm with a calendar_qc target installing strands-agents, strands-agents-tools[agent_core_browser], bedrock-agentcore, mcp, requests. MUST use aarch64-manylinux_2_17 — AgentCore Runtime is Graviton-only and the script's PLATFORM is x86_64.","status":"closed","priority":2,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T22:19:03Z","created_by":"Ross Karchner","updated_at":"2026-08-08T22:34:02Z","started_at":"2026-08-08T22:27:40Z","closed_at":"2026-08-08T22:34:02Z","close_reason":"build_lambdas.sh builds build/calendar_qc with aarch64 wheels (--only-binary :all: so a source build fails loudly rather than shipping x86_64 to Graviton). Verified ELF is ARM aarch64; 280M unpacked / 92M zipped vs 750M/250M limits. Split the old qa_agent stub arm into discovery_agent + new qa_trigger.","dependencies":[{"issue_id":"next_dctech_events-cpp.5","depends_on_id":"next_dctech_events-cpp","type":"parent-child","created_at":"2026-08-08T18:19:02Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-jg3","title":"MCP tools for reviewing and approving submissions","description":"The MCP server can create content but cannot see or act on the moderation queue, so triaging submissions requires the /edit UI. Add tools mirroring the REST admin routes: list_pending_submissions, get_submission, approve_submission (with optional trust_submitter), reject_submission, and list/trust/untrust_submitter.\n\nPromotion logic currently lives in routes/admin.py, which the MCP Lambda does not bundle. Move it into db.py so the REST handlers and MCP share one implementation rather than growing a second copy of the approval rules.\n\nAccepted trust boundary: MCP is IAM-authed for trusted agents and has no per-user admin check, unlike the REST routes. Confirmed acceptable — the endpoint already exposes add_single_event and set_overlay, so this is consistent with the existing boundary.","acceptance_criteria":"An agent can list the pending queue, inspect a draft, approve it (optionally trusting the submitter), and reject it; approval promotes to EVENT#/GROUP# and marks the draft approved exactly as the REST route does; approval logic exists in one place; trusted submitters can be listed, added, and revoked.","status":"closed","priority":2,"issue_type":"feature","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T15:30:37Z","created_by":"Ross Karchner","updated_at":"2026-08-08T15:50:10Z","started_at":"2026-08-08T15:43:47Z","closed_at":"2026-08-08T15:50:10Z","close_reason":"Implemented and verified in production: 7 moderation tools on the MCP server, promotion logic consolidated into db.promote_draft, 34 new tests","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-5ri","title":"Email the admin on every event submission","description":"Send ross@karchner.com an immediate email whenever an event or group is submitted, complementing the daily queue digest in NextOpsStack. Auto-published submissions from trusted submitters are included, since those never reach the queue and the mail is the only signal they happened.","acceptance_criteria":"Every submission path emails the admin; the mail names the event, submitter, and whether it is queued or already published; Reply-To is the submitter; an SES failure never fails the submission.","status":"closed","priority":2,"issue_type":"feature","owner":"ross@karchner.com","created_at":"2026-08-08T14:03:11Z","created_by":"Ross Karchner","updated_at":"2026-08-08T14:03:36Z","closed_at":"2026-08-08T14:03:36Z","close_reason":"Implemented and deployed; verified no send failures in production logs after two live submissions","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-v70","title":"Trusted submitters: auto-approve future submissions","description":"Add a 'trust this submitter' option to the approval flow. Once trusted, that person's later event submissions skip the moderation queue and publish immediately.\n\nTrust is keyed by normalized email, which is the only stable identity across both auth paths (magic-link submitters have submitter_id 'magiclink:\u003cemail\u003e' and no Cognito sub). This is safe because the magic-link flow proves control of the address before a submission is accepted — an attacker cannot submit as a trusted address without receiving that address's mail.\n\nAuto-approved submissions still write a DRAFT record for the audit trail, then immediately promote and mark APPROVED with an automated reviewer, so the queue history stays complete and the EVENT# write triggers the usual rebuild.","acceptance_criteria":"Approving with the trust box ticked records the submitter as trusted; that submitter's next event submission publishes without entering the queue and is visible in history as auto-approved; admins can list and revoke trusted submitters; untrusted submitters are unaffected; group submissions still require review.","status":"closed","priority":2,"issue_type":"feature","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T13:52:55Z","created_by":"Ross Karchner","updated_at":"2026-08-08T14:03:12Z","started_at":"2026-08-08T13:53:11Z","closed_at":"2026-08-08T14:03:12Z","close_reason":"Implemented and verified in production: trust checkbox on approve, auto-approval of trusted submitters' events, admin list/revoke UI, 37 new tests","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-rc3","title":"Newsletter opt-in checkbox on the event submission form","description":"Add a 'sign me up for the weekly newsletter' checkbox to the event submission form. When ticked, subscribe the submitter to the SES contact list.\n\nNo double opt-in confirmation email is needed here: the magic-link flow (or Cognito) has already proven the submitter controls that address, which is exactly what the newsletter confirmation link exists to establish. Subscribing directly avoids making the user confirm the same address twice in one flow.","acceptance_criteria":"Ticking the box subscribes the submitter to the dctech topic on the newsletter contact list; leaving it unticked subscribes nobody; an SES failure does not fail the event submission.","status":"closed","priority":2,"issue_type":"feature","owner":"ross@karchner.com","created_at":"2026-08-08T13:00:14Z","created_by":"Ross Karchner","updated_at":"2026-08-08T13:15:04Z","closed_at":"2026-08-08T13:15:04Z","close_reason":"Implemented and deployed; verified end-to-end in production","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-qd6","title":"Free-form posts on /updates with an /edit management tool","description":"Add a second kind of /updates post: hand-written, Markdown-bodied announcements (e.g. 'I've added support for recurring events'), alongside the automated weekly roundups. Free-form posts live at /updates/\u003cslug\u003e/ and interleave with weekly posts in the /updates/ index and RSS feed.\n\nAuthoring is via a new admin-gated posts manager in /edit, backed by CRUD routes under /api/admin/posts (the api_stack already proxies /api/admin/*, so no CDK route change). Drafts are visible in /edit but never rendered.","acceptance_criteria":"Admin can create/edit/delete a Markdown post in /edit; published posts render at /updates/\u003cslug\u003e/ and appear in the index and feed ordered with weekly posts; drafts never render; tests cover the model, slug handling, and draft filtering.","status":"closed","priority":2,"issue_type":"feature","owner":"ross@karchner.com","created_at":"2026-08-08T02:48:00Z","created_by":"Ross Karchner","updated_at":"2026-08-08T02:56:11Z","closed_at":"2026-08-08T02:56:11Z","close_reason":"Implemented: markdown free-form posts at /updates/\u003cslug\u003e/, admin CRUD API, /edit posts manager, draft filtering. 52 calgen tests + 12 publisher tests pass.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-i99","title":"Point updates.dctech.events at dctech.events/updates","description":"The new first-party /updates section replaces the external Bear Blog. Once the first posts are live, updates.dctech.events should redirect to https://dctech.events/updates/ so existing links and RSS subscribers follow over. Needs a Route53 record plus a redirect (CloudFront function or S3 redirect bucket) — deliberately not done as part of the feature build since it changes a live external hostname.","acceptance_criteria":"updates.dctech.events/ 301s to dctech.events/updates/; old post URLs at minimum land on the index.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-08T02:27:49Z","created_by":"Ross Karchner","updated_at":"2026-08-08T02:44:06Z","closed_at":"2026-08-08T02:44:06Z","close_reason":"Not wanted: no interest in archival content or the old domain (2026-08-08)","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-75z","title":"Add /updates weekly blog to replace updates.dctech.events","description":"Replace the external Bear Blog at updates.dctech.events with a first-party /updates section on dctech.events. A post is published for each ISO week, titled 'DC Tech Events for the week of \u003cMonday\u003e', linking to that week's /week/\u003cid\u003e/ page and summarizing the events.\n\nKey design decision: the static site is fully regenerated by calgen/Frozen-Flask every 4 hours and on DynamoDB change, so posts are DERIVED from ISO weeks rather than written by a Monday cron. A week's post becomes visible as soon as its Monday arrives — no new scheduled infrastructure, no stored post records, and the archive is reproducible from event data alone.","acceptance_criteria":"/updates/ index lists posts newest-first; /updates/\u003cy\u003e/\u003cm\u003e/\u003cd\u003e/ renders a per-week post linking to /week/\u003cid\u003e/; /updates/feed.xml is a valid RSS feed for reader migration; future weeks are never published; nav and sitemap include /updates/; tests pass.","status":"closed","priority":2,"issue_type":"feature","owner":"ross@karchner.com","created_at":"2026-08-08T02:19:30Z","created_by":"Ross Karchner","updated_at":"2026-08-08T02:27:56Z","closed_at":"2026-08-08T02:27:56Z","close_reason":"Implemented: calgen /updates index + per-week post pages + RSS, snapshot-based publisher Lambda on a Monday cron, export wiring, nav/sitemap/CSS. 49 tests pass; verified end-to-end via Frozen-Flask build and cdk synth.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-umr","title":"Add Mastodon and Bluesky credential secrets","description":"Provision two Secrets Manager secrets in a new NextSocialStack to hold externally-issued social credentials: a Mastodon API access token and Bluesky credentials (handle + app password). Values are populated manually out-of-band; CDK only creates the placeholder secrets and exposes them as stack attributes for a future posting Lambda to grant_read.","acceptance_criteria":"NextSocialStack exists with mastodon_secret and bluesky_secret attributes; wired into app.py; cdk synth succeeds; no generated values for externally-issued credentials.","status":"closed","priority":2,"issue_type":"task","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T02:16:18Z","created_by":"Ross Karchner","updated_at":"2026-08-08T02:18:21Z","started_at":"2026-08-08T02:16:29Z","closed_at":"2026-08-08T02:18:21Z","close_reason":"NextSocialStack created with mastodon + bluesky secrets (RETAIN, JSON placeholder shape), wired into app.py, synth verified","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-5ym","title":"Migration created ~31 duplicate events under mismatched guid schemes","description":"scripts/migrate_to_next_table.py reconciled the git YAML and the old dctech-events table by guid ('YAML wins on collision'), but the two sources never shared a guid scheme: calgen derives md5(date,time,title,url) while the old table's EVENT# items carried uuid-style ids. Same logical event, different key, so the collision check never fired and both copies were imported.\n\nThe migration report at the time ('62 copied verbatim') therefore understated the problem — roughly half of those 62 were duplicates of events that already existed under a calgen guid.\n\nSurfaced as 'Also published by None' on the site: calgen's remove_duplicates merged the pair at render time and credited the duplicate's group, which was null for manual events.\n\nFixed 2026-08-02: 31 redundant records deleted (backed up to .backup-predecom-20260802/deleted-duplicate-events.json), keeping the calgen-canonical record of each pair. Legitimate co-publishing between iCal groups was deliberately left intact.","acceptance_criteria":"No two EVENT# records share a title+date with no group. Any future migration reconciles on canonical event identity, not raw guid.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T23:11:12Z","created_by":"Ross Karchner","updated_at":"2026-08-02T23:11:13Z","closed_at":"2026-08-02T23:11:13Z","close_reason":"Closed","labels":["data","fixed","migration"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-48a","title":"Cognito pool is taking sustained signup spam","description":"As of 2026-08-02, 51 of 66 accounts in us-east-1_8Ay4dTt8j were UNCONFIRMED, all created within about six days. The pattern is automated abuse, not real signups:\n\n  - dot-injected Gmail aliases: l.i.z.a.g.ey.e.r.ma.n@gmail.com, r.ad.t.ech.mom.o@gmail.com, n.o.kia.fl.o.of.22@gmail.com\n  - spoofed corporate domains: questions@valvesoftware.com, paypal@wwt.it\n  - unrelated org domains: yuroktribe.nsn.us, leacounty.net, lift-texas.net\n\nOnly ~15 accounts are confirmed. The pool has selfSignUpEnabled with the hosted UI publicly reachable at login.dctech.events, so anything can register. Each signup also triggers a Cognito verification email through the SES identity, which puts the domain's sending reputation at risk if bounce rates climb.\n\nThe new weekly pruner (dctech-events-next-cleanup-unconfirmed-users) clears them after 7 days, so this is contained but not prevented. Its MAX_DELETIONS guard is set to 1000 to avoid blocking on this volume.\n\nOptions: enable the Cognito advanced-security / bot detection features, put WAF in front of the hosted UI, or turn off self-signup and invite admins manually.\n\n## Context\nFound while re-creating the pruning job that was lost when dctech-events-api was deleted. Worth checking SES bounce/complaint rates for the dctech.events identity too.","acceptance_criteria":"Either signup abuse is being blocked at the edge, or a conscious decision is recorded that pruning alone is sufficient.","notes":"Timing analysis settles what this is. Unconfirmed signups arrive 4-52 seconds apart across wholly unrelated domains (accountspayable@cbwm.org, questions@valvesoftware.com, jberling@yuroktribe.nsn.us within seconds of each other); 16 of 50 consecutive pairs are under 3 minutes. That is machine-paced, and the plausible-looking addresses are harvested third-party addresses, not registrations by their owners.\n\nSo the abuse is using Cognito as an email relay: the victims are the address owners, who receive unsolicited verification mail from dctech.events. That is what drives the complaint rate (0.07% against AWS's 0.1% review threshold), and it is why prevention beats pruning — pruning cannot unsend an email.\n\nA PreSignUp trigger is now deployed in AUDIT mode (ENFORCE=false) in NextCognitoStack. Validated against the real pool export: 0 false positives on all 15 confirmed users, catches 19 of 51 unconfirmed (37%) via gmail dot-injection and disposable domains. It cannot catch the remaining 63% because those addresses are genuinely well-formed — only rate limiting can.\n\nNext: (1) review audit logs, then set ENFORCE=true; (2) add WAF rate-based rules on the login.dctech.events hosted UI, which is now the primary control rather than a fallback.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T22:30:01Z","created_by":"Ross Karchner","updated_at":"2026-08-08T13:15:05Z","closed_at":"2026-08-08T13:15:05Z","close_reason":"Resolved by closing public sign-ups (AllowAdminCreateUserOnly=true, deployed 2026-08-08) — the SignUp endpoint bots were hitting is now rejected outright. Verified with a live SignUp call returning NotAuthorizedException.","labels":["abuse","cognito"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-0k6","title":"Port weekly cleanup of unconfirmed Cognito users","description":"The old dctech-events-api stack ran dctech-events-cleanup-unconfirmed-users on cron(0 2 ? * SUN *), deleting unconfirmed users from the shared pool us-east-1_8Ay4dTt8j. It last ran 2026-08-01 and was removed when that stack was deleted during decommissioning.\n\nThe pool is now adopted by this stack and still accumulates unconfirmed signups, so the hygiene job is still wanted. Source is in the old repo at cleanup_unconfirmed_users/handler.py; it needs cognito-idp:ListUsers and AdminDeleteUser on the pool.","acceptance_criteria":"A scheduled Lambda in this stack removes unconfirmed users older than the previous threshold, with the same weekly cadence.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T18:48:15Z","created_by":"Ross Karchner","updated_at":"2026-08-02T22:30:02Z","closed_at":"2026-08-02T22:30:02Z","close_reason":"Closed","labels":["cognito","ops"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-xyy","title":"Normalize invisible Unicode in event titles","description":"Event titles arriving from iCal feeds can contain zero-width and other invisible Unicode characters. Left in place they break two things:\n\n- skip_phrases matching, since a title with a zero-width space no longer matches the configured phrase\n- duplicate detection, since are_events_duplicates in packages/calgen/src/calgen/pipeline.py compares titles exactly, and calculate_event_hash in event_utils.py folds the title into the guid — so the same event can churn guids or fail to dedupe\n\nUpstream strips these characters in event_utils.py before hashing and in pipeline.py before comparison.\n\n## Context\nBehaviour exists in the now-archived rosskarchner/calgen at origin/main (commit 3c9246e, 'Fix invisible Unicode chars breaking skip_phrases filtering and duplicate detection'), fetchable from ~/projects/calgen. Note this changes guids for any affected event, so expect one-time churn: the aggregator will write new EVENT# items and prune the old ones. Upstream also ships tests/test_unicode_normalization.py worth porting alongside.","acceptance_criteria":"Titles differing only by invisible characters produce the same guid and dedupe against each other; skip_phrases matching is unaffected by them.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:39:36Z","created_by":"Ross Karchner","updated_at":"2026-08-02T17:15:50Z","started_at":"2026-08-02T17:15:10Z","closed_at":"2026-08-02T17:15:50Z","labels":["calgen","data-quality"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-jbv","title":"Port skip_phrases group filtering into calgen","description":"Groups can declare skip_phrases to drop matching events from their feed. The vendored calgen ignores the field entirely, so those events publish. _groups/nvtc.yaml sets:\n\n    skip_phrases:\n      - COMMUNITY TUESDAYS AT REFRACTION\n\nand roughly 15 such events are currently live on dctech.events; the pre-cutover site filtered them.\n\nUpstream implements the match in packages/calgen/src/calgen/pipeline.py during event processing, case-insensitively against the event title.\n\n## Context\nBehaviour exists in the now-archived rosskarchner/calgen at origin/main (commit ce08c53), fetchable from ~/projects/calgen. Blocked by the db.py read bug — until that is fixed the field never reaches calgen even if the matching logic is ported.","acceptance_criteria":"Events whose titles match a group's skip_phrases are absent from _data/all_events.json and the built site. The nvtc 'Community Tuesdays at Refraction' events stop appearing.","notes":"Working as intended, with a correction to this ticket's premise. The nvtc feed publishes 2 'Community Tuesdays at Refraction' copies and skip_phrases now drops both. The 13 that remain on the site are source='recurring' — a curated RECURRING# definition expanded over 90 days — which group-level skip_phrases does not (and should not) touch. That is exactly why the phrase was configured: suppress the feed duplicates, keep the curated series. Live site now shows 13, matching the pre-cutover site's 13. The ticket's '~15 such events show' figure came from a grep that conflated occurrences.","status":"closed","priority":2,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T12:39:21Z","created_by":"Ross Karchner","updated_at":"2026-08-02T17:31:13Z","started_at":"2026-08-02T17:15:51Z","closed_at":"2026-08-02T17:31:13Z","labels":["calgen","site-content"],"dependencies":[{"issue_id":"next_dctech_events-jbv","depends_on_id":"next_dctech_events-udg","type":"blocks","created_at":"2026-08-02T08:39:21Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-4zw","title":"Add automated dependency-vulnerability scanning to CI","description":"cdk_next/build_lambdas.sh installs most packages with no upper version bound, resolved fresh at build time -- reproducible today, but nothing catches a newly-disclosed CVE in cryptography, jinja2, mcp, or pyjwt between builds. Fix: add pip-audit / uv pip audit (or Dependabot) to CI.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:11:17Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:11:17Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-c4e","title":"Add a fetch-or-404 helper to cdk_next/lambda_src/mcp/server.py","description":"Every id-taking MCP tool (get_correction, get_submission, approve_submission, get_recurring_event, ...) writes its own 'if not X: raise ValueError(f\"No such ...: {id}\")' -- the pattern is hand-repeated 11 times. A tiny _get_or_404(fetch_fn, id, label) helper removes ~20 lines and guarantees a consistent error shape across every MCP tool.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:48Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:48Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-28i","title":"Deduplicate newsletter_html / newsletter_text in calgen's app.py","description":"Both routes compute window_end, call filter_events_to_upcoming_days, prepare_events_by_day, and prepare_newsletter_titles, and build the same stats dict -- differing only in which template renders and the response content-type. Extracting the shared setup removes ~15 duplicated lines and closes off the exact kind of drift this session already had to fix once (the homepage/newsletter 21-day-window bug).","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:47Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:47Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-km3","title":"Centralize env-var-with-default literals (CONTACT_LIST_NAME, FROM_EMAIL, etc.)","description":"CONTACT_LIST_NAME = os.environ.get('CONTACT_LIST_NAME', 'newsletters') appears identically in 5 files (routes/admin.py, routes/submit.py, newsletter/sender.py, newsletter/app.py, newsletter/bounce_handler.py); FROM_EMAIL's default is copy-pasted in 3. A shared constants module removes silent-drift risk -- changing a default in one file today does not propagate to the other four.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:46Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:46Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-1q0","title":"Factor a shared PythonLambda CDK construct","description":"CDK Lambda-construction boilerplate (runtime=PYTHON_3_12, timeout, handler path) is repeated 14 times across 9 stack files: ops_stack.py, api_stack.py, newsletter_stack.py, updates_stack.py, ical_aggregator_stack.py, qa_agent_stack.py, site_generator_stack.py, cognito_stack.py, and (until this session) discovery_agent_stack.py. A shared construct factory removes 5-8 repeated lines per call site and centralizes the next Python runtime bump.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:45Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:45Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-6sq","title":"Share a route-dispatch helper in cdk_next/lambda_src/api/handler.py","description":"36 hand-rolled if path == ... blocks; /api/admin/corrections/ and /api/admin/events/ each independently split the path and branch on tail == 'approve'/'reject'/empty instead of sharing one small dispatcher. ~40-60 lines of near-identical parsing would collapse into one helper; new id/approve/reject resources would stop requiring copy-pasted branching.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:45Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:45Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-5m0","title":"Split packages/calgen/src/calgen/app.py into a routes/ package","description":"app.py is a 1,507-line monolith with 31 @app.route handlers and every helper inlined (event filtering, day-grouping, feed generation, newsletter, sitemap), despite cdk_next/lambda_src/api/ already establishing a routes/ package pattern in this same repo. Splitting by concern (events/feeds/newsletter/admin-adjacent) would cut the repo's largest file by 60-70% per module.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:30Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:30Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-jwn","title":"Consolidate HTTP response builders in cdk_next/lambda_src/api/routes/*","description":"routes/responses.py already defines CORS-aware json()/html(), but routes/admin.py defines its own non-CORS _html/_json, and routes/submit.py and routes/corrections.py each locally alias routes.responses.json under the same name _json with a slightly different signature (event param). One call convention across route modules removes recurring confusion about which _json variant a given route module uses.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:29Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:29Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-kvb","title":"Extract shared magic-link JS module (submission.js / correction.js)","description":"readMagicToken, stripTokenFromUrl, and the requestLink handler (~90 lines) are copy-pasted verbatim between frontends/edit/js/submission.js and js/correction.js. A shared magic-link.js removes ~90 duplicated lines; today a fix to one (e.g. the redirect_path change from this session's corrections feature) has to be manually ported to the other.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:29Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:29Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-bit","title":"Extract shared escapeHtml() for frontends/edit/js/*.js","description":"Byte-for-byte identical escapeHtml() is reimplemented in my-submissions.js, posts.js, events.js, queue.js, subscribers.js, and corrections.js -- no shared JS module exists in frontends/edit/js/. One shared util.js removes ~30 duplicated lines and gives escaping bugs a single fix point.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:28Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:28Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-np9","title":"Remove dead legacy admin partials in cdk_next/lambda_src/api/templates/partials/","description":"12 of 17 Jinja partials there are referenced by zero route files (admin_categories.html, admin_event_edit_form.html, admin_event_override_form.html, admin_event_row.html, admin_events.html, category_edit_form.html, category_row.html, group_form.html, group_list.html, group_row.html, override_list.html, override_row.html) -- leftovers from the old server-rendered HTMX admin UI now that the live UI is the static frontends/edit/*.html + JSON API. Delete them; removes ambiguity about which admin UI is real.","status":"open","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-09-01T13:10:26Z","created_by":"Ross Karchner","updated_at":"2026-09-01T13:10:26Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-eex","title":"Two EVENT# rows store an unparsed date string","description":"Two rows hold date='June 6th 2025' and time='6pm' — free text where every other\nrow holds ISO-8601. Found 2026-08-27 while checking cpp.6 for evidence; they are\n2 of 351 EVENT# rows.\n\n  5b77f96cd548d3eab47d82af72ed25ae   manual   date='June 6th 2025' time='6pm'\n  14fe7799-eecd-5457-a883-74178e832f5b  manual  date='June 6th 2025' time=None\n\nBoth are the same event (\"Technology CFO Awards\", NVTC), so they are also a\nduplicate pair. Both are from June 2025 and absent from the site: calgen's\nload_single_events runs the string through dateparser, resolves it, and then\ndrops it as past. So there is no reader-visible effect.\n\nWHY IT IS STILL WORTH A LINE. put_event writes GSI1SK/GSI4SK from the date\nverbatim, so these rows sort as if they were in the year \"June\". Any range query\nover GSI4 — which is how get_all_events bounds a month or \"today forward\" —\ntreats them as far-future. That is exactly what made my cpp.6 evidence check\nproduce a false positive: `date \u003e= '2026-08-27'` is true for the string \"June 6th\n2025\" because \"J\" sorts after \"2\". A future date-range query could be misled the\nsame way, and the failure is silent.\n\nCause is upstream of the table: db.build_event_draft_data and MCP\nadd_single_event both take `date` as given without parsing or validating it.\ncalgen tolerates it because load_single_events parses defensively; DynamoDB does\nnot, because a sort key is a string.\n\nFIX, in rough order of value:\n  1. Validate on the way in — reject or normalize a non-ISO date in\n     build_event_draft_data and add_single_event. That is the real fix and it\n     stops the next one.\n  2. Repair or delete these two rows. They are a past duplicate pair, so\n     deleting both via MCP delete_single_event is defensible; they are also\n     harmless where they are.","status":"closed","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-27T20:15:36Z","created_by":"Ross Karchner","updated_at":"2026-08-27T21:44:34Z","closed_at":"2026-08-27T21:44:34Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-9jk","title":"Junk overlays written as booleans by an old admin form","description":"Two manual events carried the overlay {'title': True, 'hidden': True,\n'categories': True} — boolean flags where values belong. Found on 2026-08-27\nwhile deploying the overlay work, and cleared through the new DELETE\n/api/admin/events/{guid}/overlay route:\n\n  8dd82dbd997524b50e3757e2296b8ac8  CyberTalks 2026            2026-02-19\n  26fc54aaebd278cfffbb4eea5e922ac9  Test CORS Approve Event    2026-03-15\n\nNo site impact: both are past-dated and already carried a top-level\nhidden=True, so the pipeline dropped them regardless. They mattered only\nbecause step 4 made overlays apply to manual events — before that they were\ninert by accident rather than by intent.\n\nAlmost certainly written by the old forked admin form. The stranded template\ncdk_next/lambda_src/api/templates/partials/admin_event_override_form.html has\n`\u003cinput type=\"checkbox\" name=\"hidden\" value=\"true\"\u003e` alongside text inputs, and\nits route parsed form encoding, so a submit could produce truthy strings for\nevery named field. The second title (\"Test CORS Approve Event\") says test data.\n\nWHAT THIS EXPOSES, and the actual reason to file it: db.check_overlay_fields\nvalidates field *names* against OVERLAY_EDITABLE_FIELDS but not field *types*.\n`{'title': True}` is accepted today. Worth adding type checks to\n_validate_overlay_values in cdk_next/lambda_src/api/db.py — title/location/url\nand friends are strings, hidden/all_day are bools, categories is a list of\nstrings. Cheap, and it is the difference between a UI bug being caught at the\nboundary and being caught eight months later during a deploy.\n\nNothing else in the corpus is affected: the same scan found 22 events with\noverlays and only these two were malformed.","status":"closed","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-27T15:40:32Z","created_by":"Ross Karchner","updated_at":"2026-08-27T19:56:35Z","closed_at":"2026-08-27T19:56:35Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-ubw","title":"QC polish pass should correct improbable city/state pairs","description":"Ask the QC polish pass to catch locations whose city and state cannot both be\nright — \"Arlington, DC\" (Arlington is in Virginia), \"Bethesda, VA\", \"Reston,\nMD\".\n\nWHY IT MATTERS: this is not cosmetic, unlike the doubled-city issue in\nnext_dctech_events-8so. `location_utils.extract_location_info` reads the\ntrailing `City, ST`, so a wrong state silently files the event under the wrong\nregion facet:\n\n  extract_location_info(\"… Arlington, DC, Arlington, DC\") -\u003e ('Washington','DC')\n\nThe event then appears under /locations/washington-dc/ instead of Northern\nVirginia, and a reader filtering by region never sees it.\n\nHOW COMMON: rare. I scanned all 338 EVENT# items (2026-08-26) against a table\nof DC-area cities and found exactly one:\n\n  dd0d5c82  \"DevFest DC 2026 - Build Edition\"  (Google Developer Group DC)\n            \"3351 Fairfax Dr, 3351 Fairfax Drive, Arlington, DC, Arlington, DC\"\n\nSource is Meetup's own addressRegion, so this is upstream content error rather\nthan a pipeline bug — nothing to normalize, it needs judgement. Filed P3 on\nfrequency, not on impact: worth having as a standing check because it fails\nsilently, but it is not urgent at one event.\n\nThat one event is also half of the duplicate pair 8ea07ae7 / dd0d5c82, so the\ntriage pass may hide it before polish ever looks at it. Do not treat its\ndisappearance as evidence this is fixed.\n\nIMPLEMENTATION: a rule in POLISH_PROMPT (cdk_next/agents/calendar_qc/prompt.py).\nNeeds care, because the prompt currently says:\n\n  \"never change a location to move an event between cities — that is the\n   out-of-area pass's decision, not yours\"\n\nCorrecting Arlington, DC -\u003e Arlington, VA does not move the event; it labels\nthe same place correctly. The rule needs an explicit carve-out saying so, or\nthe model will read the existing caution and leave it alone. Keep the\nprohibition on actually relocating an event.\n\nWorth pairing with a deterministic check instead of or alongside the prompt: a\ncity -\u003e state table for the DC metro is small, and a mismatch is a fact rather\nthan a judgement. A validator in the aggregator or the MCP's set_overlay path\nwould catch it without a model call. The scan that found this used ~26 cities.","status":"closed","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-26T14:33:52Z","created_by":"Ross Karchner","updated_at":"2026-08-27T20:14:09Z","closed_at":"2026-08-27T20:14:09Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-cpp.6","title":"QC: duplicates that appear after approval are never re-checked","description":"An event that becomes a duplicate AFTER approval (feed edits its title/date into\ncollision) never re-enters the pending_qa queue, since the aggregator preserves\nreview_status on refresh. The corpus-comparison design catches new-vs-existing\nbut not this. Fix if it ever bites: a periodic full sweep ignoring the queue.\nNot worth building without evidence it happens.\n\nCHECKED 2026-08-27, no evidence found. Closed on this bead's own criterion.\n\nMethod: ran calgen's own are_events_duplicates over every future EVENT# row that\nwas approved, not already marked duplicate_of, and not hidden — 174 of 230\nfuture events — across all pairs.\n\nOne pair matched, and it was a false positive in the check rather than a real\ndrift: both rows are \"Technology CFO Awards\" with date stored as the *string*\n\"June 6th 2025\". A lexical `date \u003e= today` filter admits them because \"J\" sorts\nafter \"2\". They are a June 2025 event, absent from the site (0 listings), and\ncalgen drops them at build time once dateparser resolves the string. Corrected\nfor that, the count is zero.\n\nWorth knowing when this is revisited: even an unmarked duplicate pair is merged\nat render anyway. calgen's remove_duplicates runs are_events_duplicates over the\nwhole corpus independently of duplicate_of, so post-approval drift costs a\nmissing explicit mark rather than a doubled listing. That lowers the stakes\nenough that a periodic sweep would be tidying rather than fixing.\n\nIf evidence does turn up, the cheap version is a scheduled pass that queries\nGSI5 for review_status=approved and re-runs the corpus comparison — no new index\nneeded. The expensive version, re-queueing on refresh, would put every edited\nevent back through the agent weekly.\n\nFiled separately: next_dctech_events-eex, the two non-ISO date rows this turned\nup.","status":"closed","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-08T22:19:04Z","created_by":"Ross Karchner","updated_at":"2026-08-27T20:16:05Z","closed_at":"2026-08-27T20:15:37Z","close_reason":"Closed","dependencies":[{"issue_id":"next_dctech_events-cpp.6","depends_on_id":"next_dctech_events-cpp","type":"parent-child","created_at":"2026-08-08T18:19:03Z","created_by":"Ross Karchner","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-6cx","title":"Cross-post the Monday /updates post to Mastodon and Bluesky","description":"NextSocialStack already provisions the mastodon and bluesky credential secrets, and the updates publisher already produces exactly the content worth syndicating (title + link to the week). Extend the publisher (or add a follow-on target on the same Monday rule) to post the week's headline and /updates/ permalink to both networks after a successful publish.\n\nGrant with social.mastodon_secret.grant_read(fn) / social.bluesky_secret.grant_read(fn); populate the secret values first (see the NextSocialStack docstring).","acceptance_criteria":"A successful Monday publish posts once to each network; a failure to post does not block or duplicate the DynamoDB write.","status":"closed","priority":3,"issue_type":"feature","assignee":"Ross Karchner","owner":"ross@karchner.com","created_at":"2026-08-08T02:27:50Z","created_by":"Ross Karchner","updated_at":"2026-08-10T21:10:04Z","started_at":"2026-08-10T20:53:14Z","closed_at":"2026-08-10T21:10:04Z","close_reason":"Stream-driven social publisher deployed; verified live on both networks","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-p63","title":"Backfill of past /updates weeks is not possible from current data","description":"The updates publisher snapshots a week from the site's events.json, but calgen's pipeline and get_events() both drop events dated before today, so events.json only ever contains upcoming events. Invoking the publisher with {\"week_of\": \u003cpast Monday\u003e} therefore produces an empty post.\n\nThe archive can only build forward from the first real run. If historical posts matter, the content would have to come from the old Bear Blog export at updates.dctech.events, or from a DynamoDB point-in-time restore, and be written as UPDATE#\u003cweek_id\u003e items directly.","acceptance_criteria":"Decide whether historical posts are needed; if so, import them from the old blog export as UPDATE# items.","status":"closed","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-08T02:27:48Z","created_by":"Ross Karchner","updated_at":"2026-08-08T02:44:06Z","closed_at":"2026-08-08T02:44:06Z","close_reason":"Not wanted: no interest in archival content or the old domain (2026-08-08)","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-u51","title":"Drop deprecated CDK APIs: Route53 delete_existing and Stack.add_dependency","description":"cdk synth emitted 8 deprecation warnings for aws_route53.RecordSetOptions#deleteExisting and one for Stack#addDependency. delete_existing existed only for the 2026-08-01 GitHub Pages cutover; it is now a live custom resource holding route53:DELETE on the production zone. Removed it and switched add_dependency to add_stack_dependency.","status":"closed","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-06T11:45:53Z","created_by":"Ross Karchner","updated_at":"2026-08-06T11:46:49Z","closed_at":"2026-08-06T11:46:49Z","close_reason":"Fixed in b04a5e6; cdk synth clean. NextHostingStack deploy still pending.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"next_dctech_events-fkp","title":"Decide fate of the daily submission-queue notification","description":"The old stack ran dctech-events-queue-notification daily at cron(30 13 * * ? *), emailing ross@karchner.com a summary of pending submissions. It was reading the OLD dctech-events table, so since the cutover it has been reporting on a table that no longer receives submissions — it last ran 2026-08-01 and went away with the dctech-events-api stack.\n\nEither port it against dctech-events-next (GSI1 STATUS#pending) or consciously drop it. Nothing else currently notifies about a pending review queue.","acceptance_criteria":"Either a scheduled notifier exists against the new table, or this is closed as won't-do with a note.","status":"closed","priority":3,"issue_type":"task","owner":"ross@karchner.com","created_at":"2026-08-02T18:48:17Z","created_by":"Ross Karchner","updated_at":"2026-08-02T22:30:02Z","closed_at":"2026-08-02T22:30:02Z","close_reason":"Closed","labels":["ops"],"dependency_count":0,"dependent_count":0,"comment_count":0}
